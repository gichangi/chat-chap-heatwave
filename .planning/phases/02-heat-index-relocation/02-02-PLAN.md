---
phase: 02-heat-index-relocation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - nigeria_heat_index.py
autonomous: true
requirements: [HIDX-03]

must_haves:
  truths:
    - "`nigeria_heat_index.py` imports compute_relative_humidity and compute_heat_index from heatwave.science.heat_index instead of defining them inline"
    - "Zero RH/Heat-Index formula logic remains in `nigeria_heat_index.py` — no `def compute_`, no Rothfusz coefficients, no `.expression(` call"
    - "The Streamlit app still boots with no exception under streamlit.testing.v1.AppTest (the same check Phase 1 used for REWORK-08)"
    - "D-02 holds end to end: after the call-site relocation the NOAA-table Heat Index values from 02-01 still pass unchanged"
    - "D-01's clamp is now live in the app's rendering path, since the app calls the relocated clamped function"
  artifacts:
    - path: "nigeria_heat_index.py"
      provides: "Streamlit presentation layer that only imports and calls the science module"
      contains: "from heatwave.science.heat_index import"
  key_links:
    - from: "nigeria_heat_index.py"
      to: "heatwave.science.heat_index"
      via: "module-level import replacing inline defs"
      pattern: "from heatwave\\.science\\.heat_index import compute_relative_humidity, compute_heat_index"
    - from: "nigeria_heat_index.py"
      to: "era5_land pipeline"
      via: "unchanged .map() call sites"
      pattern: "\\.map\\(compute_(relative_humidity|heat_index)\\)"
---

<objective>
Point `nigeria_heat_index.py` at `heatwave.science.heat_index` and delete its inline formula
definitions, so the presentation layer's only remaining responsibility for this logic is importing and
calling it — then prove nothing regressed via the existing AppTest boot check and the full suite.

Purpose: HIDX-03. This is what makes the relocation real rather than a duplicate copy of the math.
Output: modified `nigeria_heat_index.py`; full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heat-index-relocation/02-CONTEXT.md
@.planning/phases/02-heat-index-relocation/02-PATTERNS.md
@.planning/phases/02-heat-index-relocation/02-VALIDATION.md
@.planning/phases/02-heat-index-relocation/02-01-SUMMARY.md

@nigeria_heat_index.py
@tests/test_integration.py
</context>

<interfaces>
<!-- Created by plan 02-01. Call these; do not redefine them. -->

heatwave/science/heat_index.py:
```python
def compute_relative_humidity(image: ee.Image) -> ee.Image: ...  # adds clamped 'relative_humidity' band
def compute_heat_index(image: ee.Image) -> ee.Image: ...         # adds 'heat_index' band (deg F)
```

Existing import block in nigeria_heat_index.py (lines 7-9) that the new import joins:
```python
from heatwave.config import settings
from heatwave.data.boundary import load_ward_boundary
from heatwave.data.ingest import load_era5_land
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline formula definitions with the heatwave.science.heat_index import</name>

  <files>nigeria_heat_index.py</files>

  <read_first>
    - `nigeria_heat_index.py` — current full file; the blocks to delete are lines 32-42 (`# Compute Relative Humidity` comment plus the `def compute_relative_humidity` body) and lines 46-64 (`# Compute Heat Index` comment plus the `def compute_heat_index` body); the call sites to preserve are line 44 and line 66
    - `heatwave/science/heat_index.py` — created by plan 02-01; confirm the two exported names and their signatures before wiring
    - `.planning/phases/02-heat-index-relocation/02-PATTERNS.md` — "`nigeria_heat_index.py` (modified) — controller" section, which gives the exact import line and the "call sites are unchanged" rationale
    - `tests/test_integration.py` lines 107-115 — `test_streamlit_app_boots_cleanly`, the existing regression test that verifies this task and must keep passing unmodified
  </read_first>

  <action>
    Edit `nigeria_heat_index.py` only. Do not touch `heatwave/science/heat_index.py` or any test file.

    Add `from heatwave.science.heat_index import compute_relative_humidity, compute_heat_index`
    immediately after the existing `from heatwave.data.ingest import load_era5_land` line, matching the
    surrounding absolute-import style.

    Delete both inline definition blocks entirely, including their `# Compute Relative Humidity` and
    `# Compute Heat Index` banner comments: the whole `def compute_relative_humidity(image):` body
    (currently lines 33-42) and the whole `def compute_heat_index(image):` body (currently lines 47-64).
    Nothing of the formulas — not the `100 - 5 * (T - D)` expression, not the `c1..c9` coefficient list,
    not the `273.15` conversion — may survive in this file.

    Preserve the two call sites verbatim: `relativeHumidity = era5_land.map(compute_relative_humidity)`
    and `heatIndex = relativeHumidity.map(compute_heat_index)`. They already reference the functions by
    bare name, so the new import resolves them with no further edit. Keep them in their current order and
    position relative to the `# STEP 3` / `# STEP 4` banner comments so the script's narrative flow
    survives the deletion; collapse only the blank lines left behind by the removed defs.

    Opportunistic cleanup sanctioned under CONTEXT.md's Claude's Discretion (code-review IN-02): delete
    the unused `from branca.element import Template, MacroElement` import on line 5. Verify it is dead
    before removing — `Template` and `MacroElement` appear nowhere else in the file; the legend at
    `display_heat_index_legend()` renders through `st.markdown(legend_html, unsafe_allow_html=True)`, not
    through branca. Leave every other import alone: in particular `from heatwave.auth import init_ee` on
    line 3 must stay in its current position above the `import geemap.foliumap as geemap` line, because
    Phase 1 placed it there deliberately so the `heatwave` package's `blessings` stub fires before geemap
    loads (01-01 decision). Moving or reordering it will break the app boot.

    Leave the `@st.cache_resource`-wrapped `_cached_init_ee()` (lines 12-18), the boundary/date setup, the
    slider UI, the map layers, and the legend untouched. This task changes imports and deletes dead code;
    it changes no behavior.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_integration.py::test_streamlit_app_boots_cleanly -v</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_integration.py::test_streamlit_app_boots_cleanly -v` reports 1 passed (`at.exception` is falsy)
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c "from heatwave.science.heat_index import compute_relative_humidity, compute_heat_index"` returns 1
    - `grep -v '^#' nigeria_heat_index.py | grep -c "def compute_"` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c "\-42.379"` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c "100 - 5 * (T - D)"` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c "273.15"` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -c "\.expression("` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c ".map(compute_relative_humidity)"` returns 1 and `grep -F -c ".map(compute_heat_index)"` returns 1 (call sites preserved)
    - `grep -v '^#' nigeria_heat_index.py | grep -c "branca"` returns 0 (dead import removed)
    - `grep -n "from heatwave.auth import init_ee" nigeria_heat_index.py` reports a line number lower than `grep -n "import geemap" nigeria_heat_index.py` (Phase 1 import-order constraint preserved)
  </acceptance_criteria>

  <done>nigeria_heat_index.py contains a single import line for the two science functions, zero formula logic, unchanged `.map()` call sites, and boots cleanly under AppTest.</done>
</task>

<task type="auto">
  <name>Task 2: Phase regression gate — full suite green and responsibility boundary enforced</name>

  <files>nigeria_heat_index.py</files>

  <read_first>
    - `nigeria_heat_index.py` — post-edit state from Task 1
    - `tests/test_heat_index.py` — created in plan 02-01; re-run here to prove the call-site relocation changed no numbers (D-02)
    - `tests/test_integration.py` — all seven Phase 1 tests must still pass; none of them may be edited in this phase
    - `.planning/phases/02-heat-index-relocation/02-VALIDATION.md` — sampling contract: full suite green before `/gsd:verify-work`
    - `.planning/phases/02-heat-index-relocation/02-RESEARCH.md` — "Architectural Responsibility Map": the presentation tier's only remaining responsibility for this logic is calling the science module
  </read_first>

  <action>
    Run the full suite with `.venv/Scripts/python -m pytest -v` and confirm every test passes: the seven
    Phase 1 tests in `tests/test_integration.py`, the `tests/test_requirements.py` checks, and the seven
    new tests in `tests/test_heat_index.py`. Zero failures, zero errors. Skips are acceptable only if
    GCP credentials are genuinely absent; with `keys/service_account.json` present locally, nothing in
    `tests/test_heat_index.py` or `tests/test_integration.py` should skip.

    If `tests/test_heat_index.py` now fails where it passed at the end of plan 02-01, the Task 1 edit
    changed behavior it was not supposed to change — fix `nigeria_heat_index.py`, never the test values
    or `heatwave/science/heat_index.py`'s formulas (D-02, D-03: the NOAA table values are the oracle and
    are not negotiable).

    Then confirm the architectural responsibility boundary holds by inspecting `nigeria_heat_index.py`
    with comment lines filtered out (`grep -v '^#'`): no `def compute_`, no `.expression(`, no `-42.379`,
    no `273.15`, no `100 - 5 * (T - D)`. The only permitted references to this domain are the import line
    and the two `.map(...)` call sites, plus the band-name string `'heat_index'` used by the map layer at
    `Map.addLayer(heatIndex.filter(...).select('heat_index'), visHI, "Heat Index")` and the visualisation
    dict `visHI` — those are presentation concerns, not formula logic, and stay.

    Do not modify any test file in this task. If a Phase 1 test fails, the regression is in
    `nigeria_heat_index.py` and belongs fixed there.

    Record the final pass/skip counts in the plan SUMMARY so `/gsd:verify-work` has the baseline.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest -v</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest -v` exits 0 with 0 failed and 0 errors
    - The summary line reports at least 14 passed with local credentials present (7 from `tests/test_heat_index.py`, 7 from `tests/test_integration.py`, plus `tests/test_requirements.py`)
    - `.venv/Scripts/python -m pytest tests/test_heat_index.py -v` still reports the same 7 passed as at the end of plan 02-01 — the NOAA-table values did not shift (D-02)
    - `git diff --name-only` (or equivalent working-tree diff) shows `nigeria_heat_index.py` as the only file changed by this plan; `tests/` and `heatwave/science/` are untouched
    - `grep -v '^#' nigeria_heat_index.py | grep -c "def compute_\|\.expression(\|273.15"` returns 0
    - `grep -v '^#' nigeria_heat_index.py | grep -F -c "select('heat_index')"` returns 1 (the map layer band reference is intentionally retained)
  </acceptance_criteria>

  <done>Full test suite is green, the relocated NOAA-table values are unchanged after the call-site edit, and nigeria_heat_index.py holds no formula logic — HIDX-01/02/03 are all verifiable from automated commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamlit app process -> Earth Engine | `nigeria_heat_index.py` builds and submits the server-side expression graph; the credential path (`_cached_init_ee` -> `heatwave.auth.init_ee`) is unchanged from Phase 1 |
| Slider UI input -> EE date filter | `selected_date` is formatted from three bounded `st.slider` integers, unchanged by this plan |

No new attack surface: this plan deletes code and adds one import line. It introduces no new input,
no new I/O, no new dependency, and no change to authentication or credential handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering (data integrity) | Divergent duplicate of the formulas left behind in `nigeria_heat_index.py` | mitigate | Task 2 grep gates assert `def compute_`, `.expression(`, `-42.379` and `273.15` are all absent from the file; a surviving copy would silently drift from the tested module as Phase 3 evolves it |
| T-02-07 | Tampering (data integrity) | Call-site relocation silently changing rendered Heat Index values | mitigate | Task 2 re-runs `tests/test_heat_index.py` against the NOAA table after the edit; any shift fails the gate (D-02/D-03) |
| T-02-08 | Elevation of Privilege / Spoofing | Earth Engine credential initialization in the app | accept | Untouched by this plan; `heatwave/auth.py` and the `@st.cache_resource`-wrapped `_cached_init_ee()` keep Phase 1's verified behavior, and the import-order constraint protecting the `blessings` stub is asserted by an acceptance criterion |
| T-02-09 | Information Disclosure | Streamlit legend HTML rendered with `unsafe_allow_html=True` | accept | Pre-existing, unchanged by this plan; the HTML is a static developer-authored literal with no interpolated user input. Flagged here for the Phase 5 presentation-layer rewrite rather than fixed mid-relocation |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No packages installed; `requirements.txt` is not modified by this phase (02-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_integration.py::test_streamlit_app_boots_cleanly -v` — HIDX-03 boot regression (the same AppTest check Phase 1 used for REWORK-08, re-run unmodified)
- `.venv/Scripts/python -m pytest -v` — full suite, phase gate
- `.venv/Scripts/python -m pytest tests/test_heat_index.py -v` — D-02 no-drift check after the call-site edit
</verification>

<success_criteria>
- HIDX-03: `nigeria_heat_index.py` imports and uses `heatwave.science.heat_index` instead of inline definitions, and still boots cleanly
- Phase goal met: RH/Heat Index math lives in a tested, reusable module — `heatwave/science/heat_index.py` is the single definition site, and the Streamlit script is a pure consumer
- Full test suite green, ready for `/gsd:verify-work`
</success_criteria>

<output>
Create `.planning/phases/02-heat-index-relocation/02-02-SUMMARY.md` when done
</output>
