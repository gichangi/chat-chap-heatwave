---
phase: 02-heat-index-relocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_heat_index.py
  - heatwave/science/__init__.py
  - heatwave/science/heat_index.py
autonomous: true
requirements: [HIDX-01, HIDX-02]

must_haves:
  truths:
    - "`from heatwave.science.heat_index import compute_relative_humidity, compute_heat_index` succeeds from a fresh interpreter"
    - "D-01: relative humidity is clamped to [0, 100] — dewpoint above tmean yields exactly 100, dewpoint far below tmean yields exactly 0"
    - "D-01: the clamp is applied to the single-band RH expression result only; tmax/tmean/dewpoint Kelvin bands are never clamped"
    - "D-02: the `100 - 5 * (T - D)` approximation and the nine Rothfusz coefficients are byte-identical to the originals in nigeria_heat_index.py lines 33-64"
    - "D-03: computed Heat Index matches NOAA/NWS published table values (96F/50%->108F, 100F/40%->109F, 90F/70%->105F) within +/-1.5 F"
    - "D-04: test inputs are ERA5-Land-realistic Kelvin tmean/dewpoint values, so tests exercise the same K->C->F conversion path as production"
    - "Live-EE tests skip cleanly (not fail) when neither keys/service_account.json nor EE_SA_JSON is present"
  artifacts:
    - path: "heatwave/science/__init__.py"
      provides: "package marker so heatwave.science is importable"
      contains: ""
    - path: "heatwave/science/heat_index.py"
      provides: "compute_relative_humidity() and compute_heat_index() over ee.Image"
      exports: ["compute_relative_humidity", "compute_heat_index"]
      min_lines: 30
    - path: "tests/test_heat_index.py"
      provides: "NOAA-table Heat Index verification + D-01 clamp boundary coverage"
      min_lines: 60
  key_links:
    - from: "heatwave/science/heat_index.py"
      to: "heatwave.config.settings"
      via: "band-name lookup"
      pattern: "settings\\.bands\\.(tmean|dewpoint)"
    - from: "heatwave/science/heat_index.py"
      to: "relative_humidity band"
      via: "clamp applied pre-addBands"
      pattern: "\\.clamp\\(0, 100\\)"
    - from: "tests/test_heat_index.py"
      to: "heatwave.auth.init_ee"
      via: "explicit EE initialization before any ee.Image construction"
      pattern: "init_ee\\(\\)"
---

<objective>
Create `heatwave/science/heat_index.py` by relocating (not rewriting) the inline
`compute_relative_humidity()` and `compute_heat_index()` definitions from `nigeria_heat_index.py`
lines 33-64, applying the one sanctioned behavioral fix from D-01 (`.clamp(0, 100)` on the RH
result), and prove correctness with `tests/test_heat_index.py` written test-first against NOAA/NWS
published Heat Index table values.

Purpose: HIDX-01 + HIDX-02. Phase 3 (Climatology & Heatwave Detection) consumes this module
directly, so its public function signatures become a stable contract starting now.
Output: `heatwave/science/__init__.py`, `heatwave/science/heat_index.py`, `tests/test_heat_index.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heat-index-relocation/02-CONTEXT.md
@.planning/phases/02-heat-index-relocation/02-RESEARCH.md
@.planning/phases/02-heat-index-relocation/02-PATTERNS.md
@.planning/phases/02-heat-index-relocation/02-VALIDATION.md

@nigeria_heat_index.py
@heatwave/data/boundary.py
@heatwave/config.py
@tests/test_integration.py
</context>

<interfaces>
<!-- Contracts this plan establishes. Phase 3 and plan 02-02 build against these exactly. -->

heatwave/science/heat_index.py (new, this plan):
```python
def compute_relative_humidity(image: ee.Image) -> ee.Image: ...  # adds 'relative_humidity' band, clamped [0,100]
def compute_heat_index(image: ee.Image) -> ee.Image: ...         # adds 'heat_index' band (deg F); requires 'relative_humidity' band present
```

heatwave/config.py (existing, unchanged — band-name accessors consumed by the above):
```python
settings.bands.tmax      # "temperature_2m_max"
settings.bands.tmean     # "temperature_2m"
settings.bands.dewpoint  # "dewpoint_temperature_2m"
```

heatwave/auth.py (existing, unchanged):
```python
def init_ee() -> None: ...  # service-account EE init; idempotent; undecorated (no Streamlit coupling)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing NOAA-table and clamp-boundary test suite (RED)</name>

  <files>tests/test_heat_index.py</files>

  <read_first>
    - `tests/test_integration.py` — the Phase 1 credential-gating pattern to replicate: `_KEY_FILE`/`_HAS_CREDENTIALS` computation (lines 16-22), per-test local imports and explicit `init_ee()` call (lines 60-66), and `.getInfo()`-before-assert convention (lines 66, 87, 89-91)
    - `.planning/phases/02-heat-index-relocation/02-PATTERNS.md` — "tests/test_heat_index.py (test, live-EE skip-gated)" section: full recommended test-file content and the NOAA fixture table
    - `.planning/phases/02-heat-index-relocation/02-RESEARCH.md` — Pitfalls 2, 3 and 4 (tolerance not exact equality; explicit `init_ee()` not lazy init; `.getInfo()` before comparing) and the Kelvin fixture derivation
    - `heatwave/config.py` — the `Bands` dataclass and `settings.bands.*` accessor names used by the test image builder
    - `nigeria_heat_index.py` — lines 33-64, the exact formulas under test
  </read_first>

  <behavior>
    - test_science_module_exports (HIDX-01): `from heatwave.science.heat_index import compute_relative_humidity, compute_heat_index` succeeds and both names are callable. NOT credential-gated — must run in environments without GCP credentials.
    - test_heat_index_matches_noaa_table_96_50 (D-03/D-04): tmean_k=308.706, dewpoint_k=298.706 (T=96 F, T-D=10 => RH=50%) -> heat_index == pytest.approx(108, abs=1.5)
    - test_heat_index_matches_noaa_table_100_40 (D-03/D-04): tmean_k=310.928, dewpoint_k=298.928 (T=100 F, T-D=12 => RH=40%) -> heat_index == pytest.approx(109, abs=1.5)
    - test_heat_index_matches_noaa_table_90_70 (D-03/D-04): tmean_k=305.372, dewpoint_k=299.372 (T=90 F, T-D=6 => RH=70%) -> heat_index == pytest.approx(105, abs=1.5)
    - test_relative_humidity_clamped_above_100 (D-01): tmean_k=290.0, dewpoint_k=300.0 (T-D=-10 => unclamped RH=150) -> relative_humidity == 100
    - test_relative_humidity_clamped_below_0 (D-01): tmean_k=310.0, dewpoint_k=280.0 (T-D=30 => unclamped RH=-50) -> relative_humidity == 0
    - test_source_bands_not_clamped (D-01 guard, Pitfall 1): on the clamped-above-100 image, the returned image's `settings.bands.tmean` band still reads 290.0 (not truncated to 100), proving the clamp hit the single-band RH result and not the multi-band composite
  </behavior>

  <action>
    Create `tests/test_heat_index.py`. This task is deliberately RED-first: the module under test does
    not exist yet and every live test must fail with `ModuleNotFoundError: No module named 'heatwave.science'`.
    Do NOT create `heatwave/science/` in this task — Task 2 owns that.

    Module header, copied structurally from `tests/test_integration.py` lines 9-22: a one-line module
    docstring naming HIDX-02 and the NOAA reference, `from __future__ import annotations`, then
    `import os`, `from pathlib import Path`, `import ee`, `import pytest`. Compute
    `_KEY_FILE = Path(__file__).resolve().parent.parent / "keys" / "service_account.json"` and
    `_HAS_CREDENTIALS = _KEY_FILE.exists() or bool(os.getenv("EE_SA_JSON"))` — identical expressions
    to `tests/test_integration.py`.

    Credential gate: bind it as a named decorator
    `_REQUIRES_CREDENTIALS = pytest.mark.skipif(not _HAS_CREDENTIALS, reason="Live GCP credentials not available (keys/service_account.json or EE_SA_JSON)")`
    — same predicate and same reason string as `test_integration.py`'s module-level `pytestmark` — and
    apply it as a decorator to each live-EE test. Deliberate, documented scoping choice: module-level
    `pytestmark` would also skip the credential-free HIDX-01 export assertion, which 02-VALIDATION.md
    requires to be runnable via `pytest tests/test_heat_index.py -k export`. Add a short comment in the
    file stating this so a future reader does not "restore" the module-level form. This is the same
    `pytest.mark.skipif` pattern CONTEXT.md asks for, applied per-test rather than per-module.

    Add a private helper `_make_test_image(tmean_k: float, dewpoint_k: float) -> ee.Image` that imports
    `settings` from `heatwave.config` locally, then returns
    `ee.Image.constant([tmean_k, 0, dewpoint_k]).rename([settings.bands.tmax, settings.bands.tmean, settings.bands.dewpoint])`.
    The middle placeholder 0 occupies the `tmax` slot, which none of these formulas read.

    Add a private helper `_band_value(image, band_name)` that performs
    `image.select(band_name).reduceRegion(reducer=ee.Reducer.first(), geometry=ee.Geometry.Point([0, 0]), scale=1000).get(band_name).getInfo()`
    and returns the concrete Python number. Every assertion must go through this helper — never compare
    an `ee.Image`/`ee.Number` graph object directly (RESEARCH.md Pitfall 4).

    Write the seven tests named and parameterised exactly as listed in `<behavior>` above. Each live test
    body follows `test_integration.py`'s in-function import convention: `from heatwave.auth import init_ee`,
    `from heatwave.science.heat_index import ...`, then `init_ee()` before constructing any `ee.Image`
    (RESEARCH.md Pitfall 3 — never rely on Earth Engine's implicit lazy `Initialize()`, which would use a
    different, non-service-account credential path).

    Heat Index assertions use `pytest.approx(expected, abs=1.5)`, never `==` (RESEARCH.md Pitfall 2: the
    Rothfusz regression is a curve fit to Steadman's table and differs from published entries by roughly
    0.3-1 F). Clamp assertions use exact `== 100` / `== 0`, since clamping is exact.

    Each Heat Index test docstring must cite the NOAA source for its expected value per D-03
    (weather.gov/arx/heat_index for 96/50, noaa.gov/jetstream/synoptic/heat-index for 100/40, the NWS
    table reproduction for 90/70) so no future reader mistakes these for self-derived values.

    Do not add mocks or fakes — these are live-EE tests by design, matching Phase 1's D-06/D-07 decision.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heat_index.py -v 2>&amp;1 | grep -c "No module named 'heatwave.science'"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heat_index.py -v` collects 7 tests with zero collection errors
    - At least 6 of those tests fail with `ModuleNotFoundError: No module named 'heatwave.science'` (RED state confirmed; the exact failure mode proves the tests reach the module under test rather than erroring on their own setup)
    - `grep -c "pytest.approx" tests/test_heat_index.py` returns 3 (one per NOAA table entry)
    - `grep -c "abs=1.5" tests/test_heat_index.py` returns 3
    - `grep -F -c "init_ee()" tests/test_heat_index.py` returns at least 6 (one explicit init per live test)
    - `grep -F -c ".getInfo()" tests/test_heat_index.py` returns at least 1 and every assertion of a numeric value routes through `_band_value`
    - `grep -F -c "EE_SA_JSON" tests/test_heat_index.py` returns 1
    - `grep -F -c "308.706" tests/test_heat_index.py`, `"310.928"`, and `"305.372"` each return 1 (D-04 Kelvin fixtures present verbatim)
    - `heatwave/science/` does not exist yet: `ls heatwave/science` exits non-zero
  </acceptance_criteria>

  <done>tests/test_heat_index.py exists with 7 tests that fail for the single correct reason (missing heatwave.science module), encoding NOAA table expectations and D-01 clamp boundaries before any implementation exists.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate RH/Heat-Index math into heatwave/science/heat_index.py with the D-01 clamp (GREEN)</name>

  <files>heatwave/science/__init__.py, heatwave/science/heat_index.py</files>

  <read_first>
    - `nigeria_heat_index.py` lines 32-66 — the exact source of the relocation, including the `100 - 5 * (T - D)` expression string, the nine `c1..c9` coefficients, and the `.set('system:time_start', ...)` call
    - `heatwave/data/boundary.py` — the canonical module style to replicate: one-line docstring, `from __future__ import annotations`, `import ee`, blank line, `from heatwave.config import settings`, two blank lines, plain annotated top-level functions, no classes
    - `heatwave/data/ingest.py` — the `settings.bands.*` accessor usage pattern
    - `heatwave/data/__init__.py` — confirm it is a zero-byte file; `heatwave/science/__init__.py` must match exactly
    - `.planning/phases/02-heat-index-relocation/02-PATTERNS.md` — "Target form after relocation + D-01 fix" section, which is the expected final content of this module
    - `.planning/phases/02-heat-index-relocation/02-RESEARCH.md` — Patterns 2 and 3, Pitfall 1 (clamp ordering) and Pitfall 5 (rename must touch zero arithmetic)
    - `tests/test_heat_index.py` — written in Task 1; it is the acceptance oracle for this task
  </read_first>

  <action>
    Create `heatwave/science/__init__.py` as a zero-byte file, byte-identical in emptiness to
    `heatwave/data/__init__.py`. No docstring, no content. `pyproject.toml` already declares
    `include = ["heatwave*"]` under `[tool.setuptools.packages.find]`, so no packaging change is needed.

    Create `heatwave/science/heat_index.py` with the module header exactly in `heatwave/data/boundary.py`'s
    style: a one-line docstring such as "Relative humidity and Heat Index computation over ERA5-Land
    ee.Image bands.", then `from __future__ import annotations`, blank line, `import ee`, blank line,
    `from heatwave.config import settings`, then two blank lines before the first function.

    Define `compute_relative_humidity(image: ee.Image) -> ee.Image` with a docstring noting the [0, 100]
    clamp and citing D-01/WR-01. Body: `T = image.select(settings.bands.tmean)`,
    `D = image.select(settings.bands.dewpoint)`, then build `rh` as
    `image.expression('100 - 5 * (T - D)', {'T': T, 'D': D})` chained through `.clamp(0, 100)` and then
    `.rename('relative_humidity')`, and return `image.addBands(rh)`.

    D-01 clamp placement is the one thing that must not be gotten wrong: `.clamp(0, 100)` goes on the
    single-band expression result, inside the `rh` chain, before `.rename()` and before `addBands`.
    `ee.Image.clamp` clamps ALL bands of the image it is called on (verified against
    earthengine-api 1.6.8, ee/image.py:1333), so calling it on `image` — either before or after
    `addBands` — would truncate the Kelvin `tmax`/`tmean`/`dewpoint` bands (~250-320 K) down to 100 and
    silently corrupt everything downstream. `image.clamp(` must appear nowhere in this file. Leave a
    short inline comment on the clamp line recording that it is the D-01 fix and must stay single-band.

    Define `compute_heat_index(image: ee.Image) -> ee.Image` with a docstring noting it adds a
    'heat_index' band in degrees F via the Rothfusz regression. Body, relocated verbatim from
    `nigeria_heat_index.py` lines 47-64 with the single permitted identifier rename: name the first local
    `tempK` instead of `tempC` (IN-01 cleanup, explicitly Claude's Discretion in CONTEXT.md — the variable
    holds Kelvin). Rename the identifier only; touch zero arithmetic. `tempK = image.select(settings.bands.tmean)`,
    `tempF = tempK.subtract(273.15).multiply(9 / 5).add(32)`, `RH = image.select('relative_humidity')`.

    Copy the nine coefficients character-for-character from the source (D-02, no formula changes):
    c1=-42.379, c2=2.04901523, c3=10.14333127, c4=-0.22475541, c5=-0.00683783, c6=-0.05481717,
    c7=0.00122874, c8=0.00085282, c9=-0.00000199. Copy the expression string
    'c1 + c2*T + c3*R + c4*T*R + c5*T**2 + c6*R**2 + c7*T**2*R + c8*T*R**2 + c9*T**2*R**2' verbatim,
    with the same substitution dict mapping T->tempF, R->RH and c1..c9, called on `tempF.expression(...)`,
    then `.rename('heat_index')`.

    Return `image.addBands(HI.set('system:time_start', image.get('system:time_start')))` verbatim. The
    `.set()` call is preserved deliberately: D-02 permits no behavioral change beyond the clamp, and
    dropping it — although RESEARCH.md flags it as likely dead (IN-03) — is a change this phase did not
    sanction. Leave it.

    Do not add new parameters, keyword arguments, helper classes, or a combined pipeline function. Phase 3
    will import these two names with these exact signatures.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heat_index.py -v</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heat_index.py -v` reports 7 passed, 0 failed (with local credentials present)
    - `.venv/Scripts/python -c "from heatwave.science.heat_index import compute_relative_humidity, compute_heat_index; print(compute_relative_humidity.__name__, compute_heat_index.__name__)"` prints `compute_relative_humidity compute_heat_index`
    - `heatwave/science/__init__.py` is 0 bytes: `wc -c < heatwave/science/__init__.py` returns 0
    - `grep -v '^#' heatwave/science/heat_index.py | grep -F -c ".clamp(0, 100)"` returns 1
    - `grep -v '^#' heatwave/science/heat_index.py | grep -c "image\.clamp("` returns 0 (the multi-band clamp trap is absent)
    - `grep -v '^#' heatwave/science/heat_index.py | grep -F -c "100 - 5 * (T - D)"` returns 1
    - `grep -v '^#' heatwave/science/heat_index.py | grep -F -c "\-42.379"` returns 1 and `grep -F -c "\-0.00000199"` returns 1 (coefficients relocated intact)
    - `grep -v '^#' heatwave/science/heat_index.py | grep -F -c "273.15"` returns exactly 1 (no accidental double K->C conversion introduced by the tempC->tempK rename — RESEARCH.md Pitfall 5)
    - `grep -v '^#' heatwave/science/heat_index.py | grep -c "tempC"` returns 0
    - `grep -v '^#' heatwave/science/heat_index.py | grep -c "^class "` returns 0 (plain-function style matches heatwave/data/)
    - `grep -v '^#' heatwave/science/heat_index.py | grep -F -c "from __future__ import annotations"` returns 1
  </acceptance_criteria>

  <done>heatwave/science/heat_index.py exports both functions in the codebase's established plain-function style, the full Task 1 test suite is green including all three NOAA table values and both clamp boundaries, and no source temperature band is clamped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERA5-Land raster values -> science module | Upstream pixel values (including physically impossible combinations such as dewpoint > tmean) cross into deterministic numeric math whose output feeds Phase 3 climatology and the Phase 4 CHAP deliverable |
| Test runner -> GCP service account | `tests/test_heat_index.py` reads `keys/service_account.json` (or `EE_SA_JSON`) and issues live Earth Engine calls against project `heatwave-508110` |

No new attack surface is introduced by this plan: it relocates existing server-side Earth Engine
expression math into a module, adds one range clamp, and adds a test file. There are no new external
inputs, no new network or filesystem I/O beyond what `tests/test_integration.py` already performs, no
credential-handling changes, and no packages installed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (data integrity, non-adversarial) | `compute_relative_humidity` in `heatwave/science/heat_index.py` | mitigate | `ee.Image.clamp(0, 100)` on the single-band RH expression result before `addBands` (D-01/WR-01); enforced by `test_relative_humidity_clamped_above_100` and `test_relative_humidity_clamped_below_0` |
| T-02-02 | Tampering (data integrity) | Clamp misapplied to the multi-band composite | mitigate | Acceptance criterion asserts `image.clamp(` is absent from the module; `test_source_bands_not_clamped` proves `tmean` still reads 290.0 K after RH clamping |
| T-02-03 | Information Disclosure | Service-account credential path in `tests/test_heat_index.py` | mitigate | Test performs an existence check on `keys/service_account.json` only — never reads, logs, or asserts the credential contents; `keys/` is already in `.gitignore`; identical to the Phase 1 pattern in `tests/test_integration.py` |
| T-02-04 | Denial of Service | Live Earth Engine quota consumed by tests | accept | Six `reduceRegion` calls on 3-band constant images against a dev project; negligible quota, developer-run only, skip-gated when credentials are absent |
| T-02-05 | Spoofing / Repudiation / Elevation of Privilege | — | accept | Not applicable: no authentication, authorization, session, logging, or privilege boundary is created or modified by this plan; `heatwave/auth.py` is untouched |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No packages are installed by this phase (02-RESEARCH.md Package Legitimacy Audit: not applicable — `earthengine-api`, `pytest`, `streamlit` already present and unchanged in `requirements.txt`). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_heat_index.py -v` — 7 passed with credentials present; 6 skipped + 1 passed (the export test) when credentials are absent
- `.venv/Scripts/python -m pytest -v` — full suite still green; Phase 1's `tests/test_integration.py` is unaffected because `nigeria_heat_index.py` is not touched by this plan
- `.venv/Scripts/python -c "import heatwave.science.heat_index"` — module imports cleanly outside pytest
</verification>

<success_criteria>
- HIDX-01: `heatwave/science/heat_index.py` exports `compute_relative_humidity` and `compute_heat_index`, relocated from `nigeria_heat_index.py` with only the D-01 clamp and the `tempC`->`tempK` identifier rename changed
- HIDX-02: `tests/test_heat_index.py` verifies the Rothfusz Heat Index against three independently published NOAA/NWS table values (D-03) using ERA5-Land-realistic Kelvin inputs (D-04) and a +/-1.5 F tolerance
- D-01 is directly covered by both clamp-boundary tests plus a source-band-integrity test; D-02 is enforced by the coefficient and expression-string grep assertions
- `nigeria_heat_index.py` is untouched by this plan (its update is plan 02-02's exclusive responsibility)
</success_criteria>

<output>
Create `.planning/phases/02-heat-index-relocation/02-01-SUMMARY.md` when done
</output>
