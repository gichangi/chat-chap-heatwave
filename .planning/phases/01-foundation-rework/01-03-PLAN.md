---
phase: 01-foundation-rework
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [tests/test_integration.py, tests/test_requirements.py]
autonomous: true
requirements: [REWORK-01, REWORK-02, REWORK-03, REWORK-04, REWORK-05, REWORK-06, REWORK-07, REWORK-08]

must_haves:
  truths:
    - "D-06, D-07: pytest tests/test_integration.py persists re-verification as automated tests (not a one-off script), running live checks against the heatwave-508110 GCP project when keys/service_account.json or EE_SA_JSON is present, and skips cleanly (not errors) via a credential-presence skip condition when absent"
    - "load_ward_boundary() is verified to return exactly 4,841 ward features with wardname, wardcode, lganame, statename, and geozone properties"
    - "load_era5_land() is verified to return correctly date-filtered, boundary-clipped, multi-band images"
    - "heatwave.config.settings is verified to load config.yaml into the typed Settings dataclass correctly"
    - "streamlit run nigeria_heat_index.py boots without raising an exception"
    - "requirements.txt is verified, independent of live credentials, to no longer contain ee==0.2"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "Skip-gated live integration tests for REWORK-01/02/04/05/06/07/08"
      contains: "pytestmark = pytest.mark.skipif"
    - path: "tests/test_requirements.py"
      provides: "Credential-independent static check for REWORK-03"
      contains: "ee==0.2"
  key_links:
    - from: "tests/test_integration.py"
      to: "heatwave.data.boundary.load_ward_boundary"
      via: "direct call, asserts .size().getInfo() == 4841"
      pattern: "load_ward_boundary\\(\\)"
    - from: "tests/test_integration.py"
      to: "streamlit.testing.v1.AppTest"
      via: "AppTest.from_file(str(nigeria_heat_index.py path)).run()"
      pattern: "AppTest\\.from_file"
---

<objective>
Persist re-verification of the fixed foundation (auth, config, boundary, ingest, Streamlit boot) as
automated, skip-gated integration tests per D-06/D-07, closing out REWORK-01, REWORK-02, REWORK-04,
REWORK-05, REWORK-06, REWORK-07, and REWORK-08. Also add a credential-independent static regression
check for REWORK-03 (ee==0.2 removal) so it is enforced on every test run, not just verified once
manually.

Per D-06, re-verification is persisted as automated tests in `tests/test_integration.py` (not a
one-off script), executing real Earth Engine calls against the live `heatwave-508110` project
rather than mocks. Per D-07, these tests carry a skip condition keyed on
`keys/service_account.json` / `EE_SA_JSON` presence so a contributor without live credentials is
not blocked, while the tests still actually execute (never mocked) whenever credentials are
available.

Purpose: Prove the fixes made in Plan 01-01 and Plan 01-02 actually work against the live
`heatwave-508110` project (not mocked), and give future contributors a fast, real safety net before
this branch supersedes PR #1.
Output: `tests/test_integration.py`, `tests/test_requirements.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-rework/01-CONTEXT.md
@.planning/phases/01-foundation-rework/01-RESEARCH.md
@.planning/phases/01-foundation-rework/01-PATTERNS.md
@.planning/phases/01-foundation-rework/01-VALIDATION.md

<interfaces>
<!-- Skip-condition idiom, verified against installed pytest==8.4.1 (Pitfall 5: keep it
     cheap/side-effect-free, evaluated once at collection time) -->
_KEY_FILE = Path(__file__).resolve().parent.parent / "keys" / "service_account.json"
_HAS_CREDENTIALS = _KEY_FILE.exists() or bool(os.getenv("EE_SA_JSON"))
pytestmark = pytest.mark.skipif(not _HAS_CREDENTIALS, reason="Live GCP credentials not available (keys/service_account.json or EE_SA_JSON)")

<!-- streamlit.testing.v1.AppTest, verified against installed streamlit==1.49.1 -->
from streamlit.testing.v1 import AppTest
AppTest.from_file(script_path: str, default_timeout: float) -> AppTest
at.run() -> AppTest  # mutates in place; at.exception is populated if the script raised

<!-- Post Plan 01-01/01-02 production interfaces this plan tests against -->
heatwave.config.settings.gcp_project_id  # "heatwave-508110"
heatwave.config.settings.ward_asset_id  # "projects/heatwave-508110/assets/shp"
heatwave.config.settings.bands.tmax/tmean/dewpoint  # "temperature_2m_max"/"temperature_2m"/"dewpoint_temperature_2m"
heatwave.config.settings.climatology.percentile  # 90
heatwave.auth.init_ee() -> None
heatwave.auth._LOCAL_KEY_FILE  # Path, absolute, from Plan 01-01
heatwave.data.boundary.load_ward_boundary() -> ee.FeatureCollection
heatwave.data.ingest.load_era5_land(boundary, start_date=None, end_date=None) -> ee.ImageCollection  # single collection, from Plan 01-02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_integration.py skeleton with auth/config tests</name>
  <files>tests/test_integration.py</files>
  <read_first>heatwave/auth.py, heatwave/config.py, heatwave/__init__.py, .planning/phases/01-foundation-rework/01-RESEARCH.md, .planning/phases/01-foundation-rework/01-PATTERNS.md</read_first>
  <action>
  Create `tests/test_integration.py` (new file; `tests/` directory does not exist yet). At module
  level: import `os`, `from pathlib import Path`, `import pytest`. Define `_KEY_FILE =
  Path(__file__).resolve().parent.parent / "keys" / "service_account.json"` and `_HAS_CREDENTIALS =
  _KEY_FILE.exists() or bool(os.getenv("EE_SA_JSON"))`. Set `pytestmark =
  pytest.mark.skipif(not _HAS_CREDENTIALS, reason="Live GCP credentials not available
  (keys/service_account.json or EE_SA_JSON)")` — this must stay a cheap, side-effect-free check
  (no call to `init_ee()` at collection time, per Pitfall 5 in 01-RESEARCH.md). This skip gate is
  the D-07 requirement: tests must not block a contributor who lacks live credentials, while still
  running for real (never mocked, per D-06) whenever credentials are present.
  Add `def test_settings_loaded():` importing `from heatwave.config import settings` and asserting:
  `settings.gcp_project_id == "heatwave-508110"`, `settings.ward_asset_id ==
  "projects/heatwave-508110/assets/shp"`, `settings.bands.tmax == "temperature_2m_max"`,
  `settings.bands.tmean == "temperature_2m"`, `settings.bands.dewpoint ==
  "dewpoint_temperature_2m"`, and `settings.climatology.percentile == 90` (REWORK-07).
  Add `def test_auth_resolves_from_any_cwd(tmp_path, monkeypatch):` that calls
  `monkeypatch.chdir(tmp_path)` to move the process CWD to an unrelated directory, then imports
  `heatwave.auth` and asserts `heatwave.auth._LOCAL_KEY_FILE.is_absolute()` is `True` and
  `heatwave.auth._LOCAL_KEY_FILE.exists()` is `True` — proving CWD-independence (REWORK-04).
  Add `def test_init_ee_idempotent():` that imports `from heatwave.auth import init_ee`, calls
  `init_ee()`, then calls `init_ee()` again, asserting the second call raises no exception (REWORK-01
  — an idempotency proxy; true "no re-auth on Streamlit rerun" is documented as a manual check in
  01-VALIDATION.md's Manual-Only Verifications table).
  </action>
  <verify>
    <automated>pytest tests/test_integration.py::test_settings_loaded tests/test_integration.py::test_auth_resolves_from_any_cwd tests/test_integration.py::test_init_ee_idempotent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_integration.py` succeeds
    - `grep -c "pytestmark = pytest.mark.skipif" tests/test_integration.py` returns 1
    - `pytest tests/test_integration.py::test_settings_loaded -x -q` passes
    - `pytest tests/test_integration.py::test_auth_resolves_from_any_cwd -x -q` passes
    - `pytest tests/test_integration.py::test_init_ee_idempotent -x -q` passes (live call, since keys/service_account.json is confirmed present per 01-RESEARCH.md Environment Availability)
  </acceptance_criteria>
  <done>tests/test_integration.py exists with a module-level skipif gate (D-07) and three passing tests covering REWORK-07, REWORK-04, and REWORK-01, executed live against the heatwave-508110 project rather than mocked (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Add data-access tests for boundary and ERA5-Land ingestion</name>
  <files>tests/test_integration.py</files>
  <read_first>heatwave/data/boundary.py, heatwave/data/ingest.py</read_first>
  <action>
  Append to `tests/test_integration.py`: `def test_load_ward_boundary():` that calls `init_ee()`
  (from `heatwave.auth`), then `from heatwave.data.boundary import load_ward_boundary`, calls
  `boundary = load_ward_boundary()`, and asserts `boundary.size().getInfo() == 4841` and that
  `boundary.first().propertyNames().getInfo()` contains all of `"wardname"`, `"wardcode"`,
  `"lganame"`, `"statename"`, `"geozone"` (REWORK-05).
  Add `def test_load_era5_land():` that calls `init_ee()`, loads the boundary via
  `load_ward_boundary()`, calls `from heatwave.data.ingest import load_era5_land`, then
  `collection = load_era5_land(boundary, "2020-01-01", "2020-01-05")`, and asserts
  `collection.size().getInfo() == 5` and that `collection.first().bandNames().getInfo()` contains
  `"temperature_2m_max"`, `"temperature_2m"`, and `"dewpoint_temperature_2m"` (REWORK-06).
  Add `def test_era5_land_bands_aligned():` reusing the same `collection` construction (or a fresh
  identical call) and asserting `len(collection.first().bandNames().getInfo()) == 3` — proving
  tmax/tmean/dewpoint exist together on every single image (no join needed, no unmatched-null risk)
  (REWORK-02). This test is the live, automated proof of D-06/D-07's persisted-and-skip-gated
  re-verification requirement as applied to the ingest.py restructure.
  </action>
  <verify>
    <automated>pytest tests/test_integration.py::test_load_ward_boundary tests/test_integration.py::test_load_era5_land tests/test_integration.py::test_era5_land_bands_aligned -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_integration.py::test_load_ward_boundary -x -q` passes
    - `grep -A6 "def test_load_ward_boundary" tests/test_integration.py | grep -c "4841"` returns 1
    - `pytest tests/test_integration.py::test_load_era5_land -x -q` passes
    - `pytest tests/test_integration.py::test_era5_land_bands_aligned -x -q` passes
    - `grep -c "filterDate(" tests/test_integration.py` returns 0 (tests call production code only, do not reimplement the old join)
  </acceptance_criteria>
  <done>tests/test_integration.py has three additional passing tests verifying load_ward_boundary() returns 4,841 wards with correct properties, and load_era5_land() returns correctly shaped, date-filtered, multi-band collections.</done>
</task>

<task type="auto">
  <name>Task 3: Add Streamlit boot test and requirements.txt regression guard</name>
  <files>tests/test_integration.py, tests/test_requirements.py</files>
  <read_first>nigeria_heat_index.py, requirements.txt, .planning/phases/01-foundation-rework/01-RESEARCH.md</read_first>
  <action>
  Append to `tests/test_integration.py`: `def test_streamlit_app_boots_cleanly():` using `from
  streamlit.testing.v1 import AppTest`, `script = Path(__file__).resolve().parent.parent /
  "nigeria_heat_index.py"`, `at = AppTest.from_file(str(script), default_timeout=30)`, `at.run()`,
  `assert not at.exception, f"App raised: {at.exception}"` (REWORK-08, Approach A from
  01-RESEARCH.md's Code Examples). If this fails specifically because `geemap.foliumap`'s
  `Map.to_streamlit()` custom HTML component is not tolerated by `AppTest` (RESEARCH.md Assumption
  A2), replace it with Approach B instead (subprocess `Popen(["streamlit", "run",
  "nigeria_heat_index.py", "--server.headless=true", "--server.port=8765"])` polled with
  `requests.get("http://localhost:8765")` up to ~15s, asserting HTTP 200 and no `"Traceback"` in
  captured stderr, exactly as documented in 01-RESEARCH.md's Code Examples section) — do not skip
  or weaken the assertion, swap the implementation approach if Approach A does not work.
  Separately, create `tests/test_requirements.py` (new file, no `pytestmark` skip gate, since this
  check needs no live credentials and must always run) with `def
  test_requirements_no_ee_decoy_package():` that reads `requirements.txt` from repo root
  (`Path(__file__).resolve().parent.parent / "requirements.txt"`), splits it into lines, and
  asserts no stripped line equals `"ee==0.2"` (REWORK-03 regression guard). Unlike
  `tests/test_integration.py`, this file is intentionally NOT skip-gated — D-07's credential-skip
  condition applies only to tests that require live GCP access, not to this credential-independent
  static check.
  </action>
  <verify>
    <automated>pytest tests/test_requirements.py -x -q</automated>
    <!-- test_streamlit_app_boots_cleanly boots the full Streamlit app in-process against
         live Earth Engine calls (default_timeout=30) and, combined with live-network
         latency, may exceed the 30s Nyquist feedback-latency target for the fast per-task
         commit loop. Run it at wave/phase gate instead: `pytest tests/test_integration.py::test_streamlit_app_boots_cleanly -x -q` -->
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_integration.py::test_streamlit_app_boots_cleanly -x -q` passes with no reported `at.exception`
    - `test -f tests/test_requirements.py` succeeds
    - `pytest tests/test_requirements.py -x -q` passes
    - `pytest tests/test_requirements.py -x -q -p no:cacheprovider` passes even when run in isolation from tests/test_integration.py (confirms no shared fixture/skip dependency)
    - `pytest -q --collect-only` from repo root lists both `tests/test_integration.py` and `tests/test_requirements.py` without needing a `pyproject.toml`/`pytest.ini` change (confirms default rootdir discovery works)
    - `pytest -q` from repo root reports all tests passed (or all-skipped for test_integration.py items if run without credentials, with test_requirements.py always passing)
  </acceptance_criteria>
  <done>tests/test_integration.py has a passing Streamlit-boot test (REWORK-08); tests/test_requirements.py exists as a standalone, always-running static check for REWORK-03; the full suite is discoverable and green from repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test process -> keys/service_account.json | Tests check the key file's existence only, never read or print its contents |
| Test process -> live Earth Engine API (heatwave-508110) | Real network calls per D-06; no mocking, so test failures could theoretically surface upstream error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-1-08 | Information Disclosure | tests/test_integration.py credential-skip check | mitigate | `_HAS_CREDENTIALS` only calls `.exists()`/`os.getenv()` presence checks — never opens, reads, or logs the key file's JSON contents or the `EE_SA_JSON` env var's value (Task 1) |
| T-1-09 | Information Disclosure | pytest failure output / assertion messages | accept | Standard pytest assertion failures only echo the values under test (ward counts, band names, settings fields) — none of which are secret; no test asserts on or prints credential objects |
| T-1-10 | Tampering | requirements.txt regression (ee==0.2 reintroduction) | mitigate | `tests/test_requirements.py::test_requirements_no_ee_decoy_package` runs unconditionally (no skip gate) on every `pytest -q` invocation, catching any future reintroduction of the namespace-colliding package (Task 3) |

</threat_model>

<verification>
`pytest -q` from repo root: with `keys/service_account.json` present (confirmed in this
environment), all tests in `tests/test_integration.py` and `tests/test_requirements.py` execute
live (none skipped) and pass. On a machine without credentials, `tests/test_integration.py`'s
tests report as skipped (not failed/errored) while `tests/test_requirements.py` still runs and
passes.
</verification>

<success_criteria>
- `pytest -q` is green (or all-skipped for live tests without credentials) from repo root with no config changes needed
- All 8 phase requirements (REWORK-01 through REWORK-08) have a corresponding automated test or static check
- No test reads or prints service-account key file contents
- Re-verification is persisted as automated tests against the live heatwave-508110 project (D-06), skip-gated on credential availability rather than blocking (D-07)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-rework/01-03-SUMMARY.md` when done
</output>
</output>
