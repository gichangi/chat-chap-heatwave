---
phase: 03-climatology-heatwave-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_heatwave_detection.py
  - heatwave/zonal.py
autonomous: true
requirements: [CLIM-05, CLIM-06]

must_haves:
  truths:
    - "`from heatwave.zonal import reduce_to_ward_daily` succeeds from a fresh interpreter with no GCP credentials present"
    - "CLIM-05: a gridded Heat Index ee.ImageCollection reduced against N ward polygons over M days yields exactly N*M rows, one per (ward, day)"
    - "CLIM-05: each output row carries the zonally-reduced band value for its own ward — two wards over spatially varying pixels receive different values"
    - "Every output feature carries `system:time_start` (millis), so `ee.Filter.calendarRange(..., 'day_of_year')` applied downstream in climatology.py succeeds instead of raising `Can't apply calendarRange filter to objects without a timestamp` (03-RESEARCH.md Pitfall 1)"
    - "Every output feature carries a 1-366 `doy` property matching `ee.Date.getRelative('day','year') + 1` (D-05 calendar-day convention)"
    - "A ward polygon too small for ERA5-Land's ~0.4% pixel-inclusion threshold still produces a row, with `value` null rather than the row silently vanishing (03-RESEARCH.md Pitfall 4)"
    - "D-04: reduce_to_ward_daily takes the ward FeatureCollection as a parameter and contains no hardcoded ward count, ward id, or date range — Phase 4 calls it unchanged at 4,841-ward scale"
    - "D-01: all live assertions run against real Earth Engine with small synthetic ee.Image/ee.FeatureCollection inputs — no mocks, no stubs"
    - "Live-EE tests skip cleanly (not fail) when neither keys/service_account.json nor EE_SA_JSON is present"
  artifacts:
    - path: "heatwave/zonal.py"
      provides: "reduce_to_ward_daily(): gridded ee.ImageCollection -> long-format per-ward-daily ee.FeatureCollection"
      exports: ["reduce_to_ward_daily"]
      min_lines: 40
    - path: "tests/test_heatwave_detection.py"
      provides: "CLIM-06 test file: credential gate, synthetic-data helpers, CLIM-05 zonal reduction coverage"
      min_lines: 110
  key_links:
    - from: "heatwave/zonal.py"
      to: "ee.Image.reduceRegions"
      via: "per-image zonal reduction against the ward FeatureCollection"
      pattern: "reduceRegions\\("
    - from: "heatwave/zonal.py"
      to: "downstream calendarRange filters in heatwave/science/climatology.py"
      via: "system:time_start set on every output feature"
      pattern: "system:time_start"
    - from: "tests/test_heatwave_detection.py"
      to: "heatwave.auth.init_ee"
      via: "explicit EE initialization inside every credentialed test"
      pattern: "init_ee\\(\\)"
---

<objective>
Create `heatwave/zonal.py`, the pipeline stage that converts gridded ERA5-Land Heat Index pixels
into a long-format per-ward-daily `ee.FeatureCollection`, and stand up
`tests/test_heatwave_detection.py` (the CLIM-06 test file) with its credential gate, synthetic-data
helpers, and CLIM-05 coverage written test-first.

Purpose: CLIM-05 + the CLIM-06 scaffold. This plan establishes the row schema
(`ward_id`, `value`, `doy`, `system:time_start`) that plans 03-02 and 03-03 consume, and it is the
`system:time_start` producer without which every downstream `calendarRange` pooling filter fails
outright (03-RESEARCH.md Pitfall 1).
Output: `heatwave/zonal.py`, `tests/test_heatwave_detection.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-climatology-heatwave-detection/03-CONTEXT.md
@.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md
@.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md
@.planning/phases/03-climatology-heatwave-detection/03-VALIDATION.md

@heatwave/data/ingest.py
@heatwave/data/boundary.py
@heatwave/science/heat_index.py
@heatwave/config.py
@tests/test_heat_index.py
</context>

<interfaces>
<!-- Contracts this plan establishes. Plans 03-02, 03-03, 03-04 and Phase 4 build against these exactly. -->

heatwave/zonal.py (new, this plan):
```python
ERA5_LAND_NOMINAL_SCALE_M: int = 11132  # verified: image.projection().nominalScale() -> 11131.949...

def reduce_to_ward_daily(
    image_collection: ee.ImageCollection,
    wards: ee.FeatureCollection,
    band: str = "heat_index",
    scale: int = ERA5_LAND_NOMINAL_SCALE_M,
    ward_id_property: str = "wardcode",
) -> ee.FeatureCollection: ...
```

Row schema of the returned FeatureCollection (the phase-wide contract):
```
ward_id            string  copied from wards[ward_id_property]
value              double  zonally-reduced band value for that ward on that day (null if the
                           polygon is below EE's ~0.4% pixel-weight inclusion threshold)
doy                int     1-366, ee.Date.getRelative('day','year') + 1  (D-05)
system:time_start  long    image date in millis  (REQUIRED by downstream calendarRange filters)
```

Existing, unchanged, consumed by this plan's tests:
```python
# heatwave/science/heat_index.py
def compute_relative_humidity(image: ee.Image) -> ee.Image: ...  # adds 'relative_humidity'
def compute_heat_index(image: ee.Image) -> ee.Image: ...         # adds 'heat_index' (deg F)

# heatwave/auth.py
def init_ee() -> None: ...  # idempotent service-account EE init
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_heatwave_detection.py with credential gate, synthetic helpers, and failing CLIM-05 zonal tests (RED)</name>

  <files>tests/test_heatwave_detection.py</files>

  <read_first>
    - `tests/test_heat_index.py` — the exact pattern to replicate: module docstring style (line 1), `_KEY_FILE`/`_HAS_CREDENTIALS` computation (lines 10-11), the `_REQUIRES_CREDENTIALS` named decorator plus the comment explaining why it is per-test and not a module-level `pytestmark` (lines 13-21), the local-import + explicit `init_ee()` convention inside each live test (lines 56-59), and the `_make_test_image` synthetic-data helper convention (lines 24-29)
    - `.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md` — the `tests/test_heatwave_detection.py` section: credential-gate block to copy, `_make_synthetic_ward_daily_fc` recommendation, `-k` selector naming requirements
    - `.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md` — Pattern 1 (zonal reduction), Pitfall 1 (`calendarRange` needs `system:time_start`), Pitfall 4 (small ward polygons yield a feature with the `mean` property absent, not null), and the Validation Architecture test map
    - `heatwave/config.py` — `settings.bands.*` and `settings.climatology.*` accessor names
    - `heatwave/science/heat_index.py` — the `'heat_index'` band name this plan's default `band` argument must match
  </read_first>

  <behavior>
    - test_zonal_module_exports (CLIM-05, NOT credential-gated): `from heatwave.zonal import reduce_to_ward_daily` succeeds and the name is callable. Must run green in an environment with no GCP credentials.
    - test_zonal_reduction_produces_one_row_per_ward_per_day (CLIM-05): 2 synthetic wards x 3 synthetic days -> output FeatureCollection size is exactly 6; the set of `ward_id` values is {"W-A", "W-B"}; the set of `doy` values is {1, 2, 3} for dates 2020-01-01/02/03.
    - test_zonal_reduction_assigns_each_ward_its_own_value (CLIM-05): with a longitude-valued Heat Index image, the ward centered at lon 3.0 reduces to approximately 3.0 + the day's offset and the ward centered at lon 8.0 to approximately 8.0 + the day's offset (pytest.approx, abs=0.2 — an ~11.1 km pixel spans ~0.1 deg of longitude, so the pixel-grid quantization is the tolerance driver).
    - test_zonal_output_supports_calendarrange_filter (CLIM-05 / Pitfall 1): applying `ee.Filter.calendarRange(2, 2, 'day_of_year')` to the output FeatureCollection returns 2 rows (one per ward) and raises no exception — this is the regression guard for the `Can't apply calendarRange filter to objects without a timestamp` failure mode.
    - test_zonal_reduction_tiny_ward_row_is_null_not_dropped (CLIM-05 / Pitfall 4): a ward polygon built from a 250 m buffered point (far below EE's ~0.4%-of-one-11.1 km-pixel weighted-inclusion threshold) still appears in the output — its row exists with the correct `ward_id` and `doy`, and its `value` reads as None. The row must not silently vanish.
  </behavior>

  <action>
    Create `tests/test_heatwave_detection.py`. This task is deliberately RED-first: `heatwave/zonal.py`
    does not exist yet, so every test must fail with `ModuleNotFoundError: No module named 'heatwave.zonal'`.
    Do NOT create `heatwave/zonal.py` in this task — Task 2 owns that file exclusively.

    Module header: a docstring reading "Live, skip-gated tests verifying zonal reduction, climatology
    and heatwave detection against synthetic time series (CLIM-01 through CLIM-06)." then
    `from __future__ import annotations`, `import os`, `from pathlib import Path`, `import ee`,
    `import pytest`.

    Credential gate: copy `tests/test_heat_index.py` lines 10-21 structurally — compute
    `_KEY_FILE = Path(__file__).resolve().parent.parent / "keys" / "service_account.json"` and
    `_HAS_CREDENTIALS = _KEY_FILE.exists() or bool(os.getenv("EE_SA_JSON"))`, then bind
    `_REQUIRES_CREDENTIALS = pytest.mark.skipif(not _HAS_CREDENTIALS, reason="Live GCP credentials not available (keys/service_account.json or EE_SA_JSON)")`.
    Apply it per-test as a decorator, never as a module-level `pytestmark`, and carry over the
    explanatory comment from `test_heat_index.py` lines 13-17 (retargeted to this file's
    `-k export` smoke tests) so a future reader does not "restore" the module-level form.

    Add three module-level private helpers. They are shared infrastructure — plans 03-02, 03-03 and
    03-04 extend this same file and reuse them, so give each a docstring naming its contract.

    Helper 1, `_make_ward_fc(specs)`: takes an iterable of `(ward_id, lon, lat, buffer_m)` tuples and
    returns an `ee.FeatureCollection` of `ee.Feature(ee.Geometry.Point([lon, lat]).buffer(buffer_m), {"wardcode": ward_id})`.
    The property key must be `"wardcode"` — that is `reduce_to_ward_daily`'s default `ward_id_property`
    and matches the real ward asset's verified property set (REWORK-05).

    Helper 2, `_make_heat_index_collection(date_offsets)`: takes an iterable of `(date_string, offset)`
    pairs and returns an `ee.ImageCollection` whose image for each date is
    `ee.Image.pixelLonLat().select("longitude").add(offset).rename("heat_index").set("system:time_start", ee.Date(date_string).millis())`.
    A longitude-valued band is used deliberately instead of `ee.Image.constant` so that spatially
    separated wards reduce to different, hand-predictable values — a constant image cannot distinguish
    wards at all. Band name must be exactly `"heat_index"` to match `heatwave/science/heat_index.py`'s output.

    Helper 3, `_props(fc, keys)`: calls `.getInfo()` once on the FeatureCollection and returns a plain
    Python list of dicts, one per feature, containing only the requested property keys (using
    `feature["properties"].get(key)` so an absent property surfaces as None rather than raising).
    Every assertion in this file must go through a `.getInfo()`-materialising helper like this one —
    never assert on an unevaluated `ee.*` graph object (03-RESEARCH.md / Phase 2 Pitfall 4 precedent).

    Write the five tests named and specified exactly as listed in `<behavior>`. Test-name substrings are
    load-bearing: `03-VALIDATION.md` and `03-RESEARCH.md` publish `pytest tests/test_heatwave_detection.py -k zonal -x`
    and `-k export` as the automated commands, so every CLIM-05 test name must contain `zonal` and the
    export smoke test name must contain `export`.

    Every credentialed test body follows `test_heat_index.py`'s convention: local imports
    (`from heatwave.auth import init_ee`, `from heatwave.zonal import reduce_to_ward_daily`) then
    `init_ee()` before constructing any `ee.*` object — never rely on Earth Engine's implicit lazy
    `Initialize()`, which uses a different, non-service-account credential path.

    Fixture values for the three multi-ward tests: wards `("W-A", 3.0, 7.0, 20000)` and
    `("W-B", 8.0, 7.0, 20000)` — the 20 km buffer keeps both polygons comfortably above EE's
    ~0.4%-pixel-weight inclusion threshold so Pitfall 4 cannot masquerade as an algorithm bug. Dates
    and offsets `[("2020-01-01", 0), ("2020-01-02", 10), ("2020-01-03", 20)]`. For the Pitfall 4 test,
    add a third ward `("W-TINY", 5.0, 7.0, 250)` and use a single date.

    Do not add mocks, fakes, or a numpy/pandas reference implementation of any of this math — D-01
    mandates live EE for every assertion and D-02 explicitly forbids a parallel reference
    implementation as a test oracle. Do not create a `conftest.py`; keep all helpers local to this file
    (03-VALIDATION.md Wave 0 Requirements).
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v 2>&amp;1 | grep -c "No module named 'heatwave.zonal'"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v` collects exactly 5 tests with zero collection errors
    - All 5 tests fail with `ModuleNotFoundError: No module named 'heatwave.zonal'` (RED confirmed — the failures reach the module under test rather than erroring in test setup)
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k zonal --collect-only -q` collects exactly 5 test ids (all five names contain `zonal`)
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k export --collect-only -q` collects exactly 1 test id
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "EE_SA_JSON"` returns 1
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -c "^pytestmark"` returns 0 (per-test gate, not module-level)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "@_REQUIRES_CREDENTIALS"` returns 4 (every test except the export smoke test)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "init_ee()"` returns at least 4
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "pixelLonLat"` returns 1 (spatially varying fixture present)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "calendarRange"` returns at least 1 (Pitfall 1 regression guard present)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "wardcode"` returns at least 1
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -c "import numpy\|import pandas\|mock"` returns 0 (D-01/D-02: no mocks, no parallel oracle)
    - `heatwave/zonal.py` does not exist yet: `ls heatwave/zonal.py` exits non-zero
  </acceptance_criteria>

  <done>tests/test_heatwave_detection.py exists with a per-test credential gate, three reusable synthetic-data helpers, and five CLIM-05 tests that fail for the single correct reason (missing heatwave.zonal module), encoding the row-schema contract before any implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement heatwave/zonal.py gridded-to-per-ward-daily reduction (GREEN)</name>

  <files>heatwave/zonal.py</files>

  <read_first>
    - `heatwave/data/ingest.py` — the canonical module style to replicate: multi-line docstring that states what this pipeline stage does and explicitly what it hands off to next (lines 1-8), `from __future__ import annotations`, `import ee`, `from heatwave.config import settings`, plain annotated top-level functions, and the `.map()`-over-ImageCollection idiom (lines 30-35). Note that line 7 of this file already names `heatwave/zonal.py` as the next stage
    - `heatwave/data/boundary.py` — the minimal plain-function, no-class module shape
    - `.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md` — Pattern 1 (the live-verified `reduceRegions` + `.flatten()` implementation), Pitfall 1 (`system:time_start` requirement), Pitfall 4 (small-polygon missing-property behaviour), and the `ERA5_LAND_NOMINAL_SCALE_M` verification note (`nominalScale()` -> 11131.949...)
    - `.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md` — the `heatwave/zonal.py` section: imports pattern, docstring convention, function-signature convention, and the core zonal-reduction pattern
    - `tests/test_heatwave_detection.py` — written in Task 1; it is this task's acceptance oracle and defines the exact row schema
    - `heatwave/science/heat_index.py` — the `'heat_index'` band name and the `system:time_start` propagation precedent on line 40
  </read_first>

  <behavior>
    - Given an ee.ImageCollection of M images each carrying `system:time_start` and an ee.FeatureCollection of N wards, `reduce_to_ward_daily` returns a flat ee.FeatureCollection of exactly N*M features.
    - Each returned feature carries `ward_id` (from the ward's `ward_id_property`), `value` (the reduced band value, or null), `doy` (1-366), and `system:time_start` (millis).
    - The returned collection can be filtered by `ee.Filter.calendarRange(d, d, 'day_of_year')` and by `ee.Filter.calendarRange(y1, y2, 'year')` without error.
    - Wards below EE's pixel-weight inclusion threshold yield a feature whose `value` is null, not an omitted feature.
  </behavior>

  <action>
    Create `heatwave/zonal.py` at the package root (not under `heatwave/data/` or `heatwave/science/` —
    CLIM-05 and 03-CONTEXT.md both name this exact path, and `heatwave/data/ingest.py` line 7 already
    forward-references it).

    Module docstring in `heatwave/data/ingest.py`'s style: state that this stage reduces gridded
    ERA5-Land Heat Index pixels to one row per (ward, day) via `ee.Image.reduceRegions`, name the exact
    output row schema (`ward_id`, `value`, `doy`, `system:time_start`), state that it hands off to
    `heatwave/science/climatology.py` and `heatwave/science/heatwave.py`, and record that
    `system:time_start` is mandatory because those downstream stages filter with
    `ee.Filter.calendarRange`, which derives day-of-year from the timestamp and errors on features
    without one. Then `from __future__ import annotations`, blank line, `import ee`, blank line,
    `from heatwave.config import settings`.

    Define a module-level constant `ERA5_LAND_NOMINAL_SCALE_M = 11132` with a comment recording that
    this is ERA5-Land's verified nominal pixel scale (`image.projection().nominalScale()` returns
    11131.949..., verified live against `heatwave-508110`, 03-RESEARCH.md Pattern 1). Do not inline
    `11132` as a bare magic number anywhere.

    Define `reduce_to_ward_daily(image_collection: ee.ImageCollection, wards: ee.FeatureCollection, band: str = "heat_index", scale: int = ERA5_LAND_NOMINAL_SCALE_M, ward_id_property: str = "wardcode") -> ee.FeatureCollection`.
    Every input is a parameter: no ward count, ward id, date range, or collection id may be hardcoded
    (D-04 — Phase 4's `scripts/run_batch_export.py` calls this same function unmodified at 4,841-ward
    scale). Docstring must state the row schema, cite CLIM-05, and carry the Pitfall 4 warning described
    below.

    Body, following 03-RESEARCH.md Pattern 1 (verified live). Inner function `reduce_one_day(image)`:
    take `date = image.date()`; compute `doy = date.getRelative("day", "year").add(1)` — the 1-366
    WMO/ETCCDI convention D-05 locks in, which also gives Feb 29 its own slot in leap years with no
    custom calendar remapping; call `image.select(band).reduceRegions(collection=wards, reducer=ee.Reducer.mean(), scale=scale)`;
    then `.map()` over the result setting `ward_id` from `feature.get(ward_id_property)`, `value` from
    `feature.get("mean")`, `doy`, and `system:time_start` from `date.millis()`. Return
    `ee.FeatureCollection(image_collection.map(reduce_one_day)).flatten()`.

    Two details that must not be gotten wrong:

    First, `system:time_start` must be set on every output feature, not just on the intermediate image.
    `reduceRegions` output features do not inherit the source image's timestamp. Omitting it produces
    `Collection.filter: Can't apply calendarRange filter to objects without a timestamp.` at
    `.getInfo()` time in `heatwave/science/climatology.py` (03-RESEARCH.md Pitfall 1, reproduced live).

    Second, normalise the reducer output to the canonical `value` property via
    `feature.get("mean")` unconditionally, and do NOT coalesce a missing result to 0 or drop the row.
    `ee.Reducer.mean()` omits the `mean` property entirely (not null, absent) for polygons below EE's
    ~0.4%-pixel-weight inclusion threshold; `.get()` on the absent key yields null, which turns silent
    absence into an explicit, detectable null downstream. Document this in the function docstring as a
    caller obligation for Phase 4 (a missing ward-day must never be silently aggregated as zero heatwave
    days — 03-RESEARCH.md Security Domain, data-integrity threat pattern).

    Reducer choice is `ee.Reducer.mean()` — the representative ward-level daily value, per 03-CONTEXT.md's
    Claude's Discretion item, confirmed against the `ee.Reducer` API during research. Expose `band` and
    `scale` as parameters so a caller can choose otherwise without editing this module.

    Do not add classes, do not add a Streamlit or pandas import, and do not compute day-of-year in
    Python via `datetime` — `ee.Date.getRelative` keeps the whole computation server-side
    (03-RESEARCH.md Don't Hand-Roll).
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v` reports 5 passed, 0 failed (with local credentials present)
    - `.venv/Scripts/python -m pytest tests/ -x -q` reports the full suite green (Phase 1 and Phase 2 tests unaffected)
    - `.venv/Scripts/python -c "from heatwave.zonal import reduce_to_ward_daily; print(reduce_to_ward_daily.__name__)"` prints `reduce_to_ward_daily`
    - `grep -v '^#' heatwave/zonal.py | grep -F -c "reduceRegions("` returns 1
    - `grep -v '^#' heatwave/zonal.py | grep -F -c "system:time_start"` returns at least 1
    - `grep -v '^#' heatwave/zonal.py | grep -F -c 'getRelative("day", "year")'` returns 1
    - `grep -v '^#' heatwave/zonal.py | grep -F -c "ERA5_LAND_NOMINAL_SCALE_M"` returns 2 (constant definition plus its use as the `scale` default)
    - `grep -v '^#' heatwave/zonal.py | grep -c "11132"` returns 1 (the named constant only — no bare magic number elsewhere)
    - `grep -v '^#' heatwave/zonal.py | grep -c "4841\|4,841"` returns 0 (D-04: no hardcoded ward count)
    - `grep -v '^#' heatwave/zonal.py | grep -c "^class "` returns 0
    - `grep -v '^#' heatwave/zonal.py | grep -F -c "from __future__ import annotations"` returns 1
    - `grep -v '^#' heatwave/zonal.py | grep -c "import numpy\|import pandas\|import datetime"` returns 0
  </acceptance_criteria>

  <done>heatwave/zonal.py exports reduce_to_ward_daily in the codebase's plain-function style, all five CLIM-05 tests are green including the calendarRange-compatibility guard and the tiny-ward null-not-dropped guard, and the full test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERA5-Land raster pixels -> per-ward table | Gridded upstream values (including no-data regions and sub-pixel ward polygons) cross into the tabular representation that every downstream climatology, detection, and CHAP-facing aggregate is computed from |
| Caller-supplied ward FeatureCollection -> reduceRegions | `wards`, `band`, `scale`, and `ward_id_property` are function parameters supplied by in-repo callers (tests now, `scripts/run_batch_export.py` in Phase 4) — trusted, internal, not externally submitted |
| Test runner -> GCP service account | `tests/test_heatwave_detection.py` reads `keys/service_account.json` (or `EE_SA_JSON`) and issues live Earth Engine calls against project `heatwave-508110` |

No new authentication, session, or access-control surface is introduced: credential handling remains
entirely `heatwave/auth.py` (Phase 1, untouched), and this phase installs no packages
(03-RESEARCH.md Package Legitimacy Audit: not applicable).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (data integrity) | `reduce_to_ward_daily` on sub-pixel ward polygons | mitigate | Normalise the absent `mean` property to an explicit null `value` rather than dropping the row or coalescing to 0; enforced by `test_zonal_reduction_tiny_ward_row_is_null_not_dropped` and documented as a Phase 4 caller obligation (03-RESEARCH.md Pitfall 4 / Security Domain) |
| T-03-02 | Tampering (silent downstream failure) | Missing `system:time_start` on output features | mitigate | Set `system:time_start` from `image.date().millis()` on every output feature; enforced by `test_zonal_output_supports_calendarrange_filter`, which is the direct regression guard for the verified `Can't apply calendarRange filter to objects without a timestamp` error |
| T-03-03 | Tampering (config drift) | Hardcoded scale / ward id / band name | mitigate | `scale` defaults to the named `ERA5_LAND_NOMINAL_SCALE_M` constant with its verification cited; `band`, `wards`, and `ward_id_property` are parameters (D-04); acceptance criteria assert no `4841` literal and exactly one `11132` occurrence |
| T-03-04 | Information Disclosure | Service-account credential path in the test file | mitigate | Existence check on `keys/service_account.json` only — contents are never read, logged, or asserted; `keys/` is already gitignored; identical to the Phase 1/2 pattern |
| T-03-05 | Denial of Service | Live Earth Engine quota consumed by tests | accept | Five tests, each a handful of `reduceRegions` calls over 1-3 synthetic images and 2-3 synthetic polygons; negligible dev-project quota, skip-gated when credentials are absent (D-01/D-03 keep the scale small) |
| T-03-06 | Spoofing / Repudiation / Elevation of Privilege | — | accept | Not applicable: no authentication, authorization, session, or privilege boundary is created or modified; `heatwave/auth.py` is untouched and V2/V3/V4 are marked "No" in 03-RESEARCH.md's Security Domain |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No packages installed by this plan — `earthengine-api` 1.6.8 and `pytest` were already present and audited in Phases 1-2 (03-RESEARCH.md Package Legitimacy Audit: not applicable). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k zonal -x` — 4 live CLIM-05 tests pass with credentials present, skip cleanly without
- `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k export -x` — 1 test passes with no credentials required
- `.venv/Scripts/python -m pytest tests/ -x` — full suite green; Phases 1-2 unaffected (no existing file is modified by this plan)
- `.venv/Scripts/python -c "import heatwave.zonal"` — module imports cleanly outside pytest
</verification>

<success_criteria>
- CLIM-05: `heatwave/zonal.py` reduces a gridded Heat Index `ee.ImageCollection` to a long-format per-ward-daily `ee.FeatureCollection` with one row per (ward, day), verified live against spatially separated synthetic wards
- CLIM-06 (partial): `tests/test_heatwave_detection.py` exists with the per-test credential gate, three reusable synthetic-data helpers, and CLIM-05 coverage — plans 03-02 and 03-03 extend this same file
- D-01 is honoured: every assertion runs against live Earth Engine on small synthetic inputs; no mocks, no stubs, no numpy/pandas oracle (D-02)
- D-04 is honoured: `reduce_to_ward_daily` is ward-count-agnostic and takes wards, band, scale, and ward-id property as parameters
- D-05 is honoured: `doy` is `getRelative('day','year') + 1`, the 1-366 convention that gives Feb 29 its own leap-year slot
- 03-RESEARCH.md Pitfall 1 and Pitfall 4 both have a dedicated live regression test
</success_criteria>

<output>
Create `.planning/phases/03-climatology-heatwave-detection/03-01-SUMMARY.md` when done
</output>
