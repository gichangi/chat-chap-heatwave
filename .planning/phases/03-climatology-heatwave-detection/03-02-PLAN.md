---
phase: 03-climatology-heatwave-detection
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_heatwave_detection.py
  - heatwave/science/climatology.py
autonomous: true
requirements: [CLIM-01, CLIM-02, CLIM-06]

must_haves:
  truths:
    - "`from heatwave.science.climatology import floor_mod, wrapped_day, pooling_window_filter, compute_climatology_thresholds` succeeds with no GCP credentials present"
    - "CLIM-01: given a per-ward-daily FeatureCollection, one percentile threshold is produced per (ward_id, day-of-year) pair, computed by ee.Reducer.percentile grouped by ward in a single reducer call per calendar day"
    - "CLIM-01: only rows whose year falls inside [baseline_start_year, baseline_end_year] contribute to a threshold — a 999.0 outlier in 1999 or 2021 does not move a 1991-2020 baseline threshold"
    - "CLIM-02: the threshold for day-of-year D pools every baseline row whose day-of-year lies within ±pooling_window_days of D"
    - "D-06: the pooling window wraps in both directions — the window for day 3 includes late-December days, and the window for day 366 includes early-January days; neither truncates at the 1/366 edge"
    - "D-06: floor_mod(-3, 366) returns 363, not the -3 that raw ee.Number.mod() returns, so no negative value ever reaches ee.Filter.calendarRange"
    - "D-05: day-of-year 60 in a leap year is Feb 29 and day-of-year 59 is Feb 28 — Feb 29 receives its own pooled threshold and is never merged into or shared with Feb 28's"
    - "D-02: the expected 90th-percentile value asserted in tests is EE's own live-computed 9.5 for the sample 1..10, explicitly not numpy's 9.1 — no parallel Python percentile implementation exists in test or production code"
    - "Security V5: percentile, pooling_window_days, baseline_start_year and baseline_end_year default to None in the signature and resolve from heatwave.config.settings.climatology — the literals 90, 5, 1991 and 2020 appear nowhere in the module"
    - "D-04: compute_climatology_thresholds handles any number of wards through ee.Reducer.group, with no per-ward Python loop and no hardcoded ward count"
  artifacts:
    - path: "heatwave/science/climatology.py"
      provides: "floor-mod wraparound helpers, the ±N-day pooling filter, and the per-ward per-calendar-day percentile threshold computation"
      exports: ["floor_mod", "wrapped_day", "pooling_window_filter", "compute_climatology_thresholds"]
      min_lines: 70
    - path: "tests/test_heatwave_detection.py"
      provides: "CLIM-01/CLIM-02 coverage added alongside the existing CLIM-05 tests"
      min_lines: 260
  key_links:
    - from: "heatwave/science/climatology.py"
      to: "heatwave.config.settings.climatology"
      via: "None-defaulting parameters resolved from config"
      pattern: "settings\\.climatology\\.(percentile|pooling_window_days|baseline_start_year|baseline_end_year)"
    - from: "heatwave/science/climatology.py"
      to: "heatwave/zonal.py output rows"
      via: "ee.Filter.calendarRange over system:time_start set by reduce_to_ward_daily"
      pattern: "calendarRange\\("
    - from: "heatwave/science/climatology.py"
      to: "ee.Reducer.percentile grouped by ward"
      via: "reduceColumns with a grouped percentile reducer"
      pattern: "percentile\\(\\[.*\\]\\)\\.group\\("
---

<objective>
Create `heatwave/science/climatology.py`: the wraparound-safe ±N-day pooling window and the per-ward,
per-calendar-day percentile threshold computation that turns a multi-year ward-daily table into the
climatological baseline every heatwave-day decision is made against.

Purpose: CLIM-01 + CLIM-02. This is where D-05 (day-of-year 1-366, Feb 29 gets its own slot) and D-06
(pooling wraps across the year boundary in both directions) become executable code, and where the
floor-mod trap that silently breaks `ee.Filter.calendarRange` is neutralised.
Output: `heatwave/science/climatology.py`, extended `tests/test_heatwave_detection.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-climatology-heatwave-detection/03-CONTEXT.md
@.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md
@.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md
@.planning/phases/03-climatology-heatwave-detection/03-VALIDATION.md
@.planning/phases/03-climatology-heatwave-detection/03-01-SUMMARY.md

@heatwave/zonal.py
@heatwave/science/heat_index.py
@heatwave/config.py
@tests/test_heatwave_detection.py
</context>

<interfaces>
<!-- Contracts this plan establishes. Plan 03-03, plan 03-04 and Phase 4 build against these exactly. -->

heatwave/science/climatology.py (new, this plan):
```python
DAYS_IN_LEAP_YEAR: int = 366

def floor_mod(n, m) -> ee.Number: ...              # Python-style floored modulo, never negative
def wrapped_day(n) -> ee.Number: ...               # any integer -> 1..366 (-3 -> 363, 367 -> 1, 730 -> 364)
def pooling_window_filter(doy, window_days) -> ee.Filter: ...   # calendarRange(start, end, 'day_of_year')

def compute_climatology_thresholds(
    ward_daily_fc: ee.FeatureCollection,
    percentile: int | None = None,           # -> settings.climatology.percentile (90)
    window_days: int | None = None,          # -> settings.climatology.pooling_window_days (5)
    baseline_start_year: int | None = None,  # -> settings.climatology.baseline_start_year (1991)
    baseline_end_year: int | None = None,    # -> settings.climatology.baseline_end_year (2020)
    value_property: str = "value",
    ward_id_property: str = "ward_id",
) -> ee.FeatureCollection: ...
```

Row schema of the returned climatology FeatureCollection (consumed by plan 03-03's join):
```
ward_id    string  ward identifier
doy        int     1-366 calendar day
threshold  double  the pooled percentile value for that (ward, day-of-year)
```
Note: climatology rows deliberately carry no `system:time_start` — they are joined on
(`ward_id`, `doy`) and are never calendarRange-filtered.

Consumed from plan 03-01 (already implemented):
```python
# heatwave/zonal.py — produces rows with ward_id, value, doy, system:time_start
def reduce_to_ward_daily(image_collection, wards, band="heat_index",
                         scale=ERA5_LAND_NOMINAL_SCALE_M, ward_id_property="wardcode") -> ee.FeatureCollection: ...
```

Consumed from heatwave/config.py (existing, unchanged):
```python
settings.climatology.baseline_start_year  # 1991
settings.climatology.baseline_end_year    # 2020
settings.climatology.percentile           # 90
settings.climatology.pooling_window_days  # 5
settings.climatology.min_consecutive_days # 3  (used by plan 03-03, not here)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing CLIM-01/CLIM-02 climatology and pooling-window tests (RED)</name>

  <files>tests/test_heatwave_detection.py</files>

  <read_first>
    - `tests/test_heatwave_detection.py` — the file as plan 03-01 left it: the `_REQUIRES_CREDENTIALS` gate, the `_make_ward_fc` / `_make_heat_index_collection` / `_props` helpers, and the existing five CLIM-05 tests. New tests append to this file; do not restructure or rename anything already there
    - `.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md` — Pattern 3 (floor-mod helper and the verified wrap values -3 -> 363, 367 -> 1, 730 -> 364), Pattern 4 (grouped percentile), Pitfall 2 (`ee.Number.mod()` is not floor-mod), Pitfall 3 (EE percentile for `[1..10]` p90 is 9.5, numpy's default is 9.1), and the live-verified doy values for 2020-02-29 (59 -> doy 60) and 2020-02-28
    - `.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md` — the `heatwave/science/climatology.py` section and the shared "Config-driven parameters" pattern
    - `.planning/phases/03-climatology-heatwave-detection/03-CONTEXT.md` — D-02, D-05, D-06 in full
    - `heatwave/config.py` — `ClimatologyConfig` field names asserted by the config-defaults test
    - `heatwave/zonal.py` — the row schema (`ward_id`, `value`, `doy`, `system:time_start`) the new synthetic helper must reproduce exactly
  </read_first>

  <behavior>
    - test_climatology_module_exports (CLIM-01, NOT credential-gated): `floor_mod`, `wrapped_day`, `pooling_window_filter`, `compute_climatology_thresholds` are all importable from `heatwave.science.climatology` and callable.
    - test_pooling_window_floor_mod_handles_negatives (D-06 / Pitfall 2): `floor_mod(-3, 366).getInfo() == 363`, `floor_mod(-1, 366).getInfo() == 365`, `floor_mod(370, 366).getInfo() == 4`, and `wrapped_day` returns 363 for -3, 366 for 0, 1 for 1, 1 for 367, 364 for 730, 366 for 366. The negative cases are the ones that matter: raw `ee.Number(-3).mod(366)` returns -3, which `ee.Filter.calendarRange` rejects with `Start and end date values must be >= 0`.
    - test_pooling_window_wraps_across_new_year (CLIM-02 / D-06): on a one-ward FeatureCollection covering 2019-12-27..2019-12-31 (doy 361-365, non-leap) and 2020-01-01..2020-01-10 (doy 1-10), filtering with `pooling_window_filter(3, 5)` (window = doy 364 through doy 8, wrapping) returns exactly 10 rows — 2019-12-30, 2019-12-31, and 2020-01-01 through 2020-01-08.
    - test_pooling_window_wraps_at_day_366 (CLIM-02 / D-05 / D-06): on the same collection, `pooling_window_filter(366, 5)` (window = doy 361 through doy 5, wrapping) returns exactly 10 rows — all five December rows and 2020-01-01 through 2020-01-05.
    - test_climatology_threshold_matches_live_verified_ee_percentile (CLIM-01 / D-02): a one-ward collection with values 1.0..10.0 on 2000-01-01..2000-01-10, called with percentile=90, window_days=5, baseline 2000-2000, yields exactly one row for doy 6 whose `threshold` is 9.5. The docstring must record that 9.5 is EE's own live-computed value (03-RESEARCH.md Pitfall 3) and that numpy's default `linear` method would give 9.1 — asserting 9.1 here would mean the test is measuring the wrong engine.
    - test_climatology_baseline_year_filter_excludes_outside_years (CLIM-01): the same 2000 collection plus two 999.0 outlier rows dated 1999-01-05 and 2021-01-05 (both inside the doy-6 pooling window), called with baseline 2000-2000, still yields threshold 9.5 for doy 6.
    - test_climatology_thresholds_are_computed_per_ward (CLIM-01 / D-04): two wards over the same ten dates — W-A with values 1.0..10.0 and W-B with 101.0..110.0 — produce, in a single call, two doy-6 rows: W-A threshold 9.5 and W-B threshold 109.5 (percentile is shift-equivariant, so the same EE rule applies). Proves the grouped reducer separates wards rather than pooling them.
    - test_climatology_feb29_is_its_own_calendar_day_slot (CLIM-01 / D-05): `ee.Date("2020-02-29").getRelative("day","year").add(1)` is 60 and `ee.Date("2020-02-28")...` is 59; and with window_days=0 on a collection holding 2020-02-28 value 1.0 and 2020-02-29 value 99.0 (baseline 2020-2020), doy 59 yields threshold 1.0 and doy 60 yields threshold 99.0 — Feb 29 is neither merged into nor fallback-shared with Feb 28.
    - test_climatology_defaults_read_from_settings (CLIM-01 / CLIM-02 / security V5): `inspect.signature(compute_climatology_thresholds)` shows `percentile`, `window_days`, `baseline_start_year`, `baseline_end_year` all defaulting to None; and calling the function with only `ward_daily_fc` on the year-2000 values-1..10 collection yields threshold 9.5 for doy 6 — which is only true if the defaults resolved to percentile 90, window 5, and a baseline range containing 2000.
  </behavior>

  <action>
    Append to `tests/test_heatwave_detection.py`. Do not modify the existing header, credential gate,
    helpers, or CLIM-05 tests from plan 03-01.

    Add `import inspect` to the module imports (needed by the config-defaults test).

    Add one new module-level helper, `_make_ward_daily_fc(rows)`, documented as producing exactly the
    row schema `heatwave/zonal.py` emits so climatology tests exercise the real contract without paying
    for zonal reduction. It takes an iterable of `(ward_id, date_string, value)` tuples and returns
    `ee.FeatureCollection` of `ee.Feature(None, {...})` objects, each carrying: `ward_id` (string),
    `value` (float), `doy` computed server-side as `ee.Date(date_string).getRelative("day", "year").add(1)`,
    and `system:time_start` as `ee.Date(date_string).millis()`. Computing `doy` from the date rather
    than hardcoding it guarantees the helper agrees with `heatwave/zonal.py`'s convention by
    construction. `system:time_start` is mandatory — every `calendarRange` filter under test reads
    day-of-year and year from it, not from the `doy` property (03-RESEARCH.md Pitfall 1).

    Write the nine tests named and specified exactly as in `<behavior>`. Name substrings are
    load-bearing: `03-VALIDATION.md` publishes `-k climatology` and `-k pooling` as the automated
    commands, so all six CLIM-01 tests (the module-export smoke test plus the five threshold-behaviour
    tests) must contain `climatology` and the three CLIM-02 tests must contain `pooling`.

    Every credentialed test body imports locally (`from heatwave.auth import init_ee`,
    `from heatwave.science.climatology import ...`) and calls `init_ee()` before constructing any
    `ee.*` object, matching the established convention.

    Assertions on `compute_climatology_thresholds` output must filter the returned collection down to
    the row under test before calling `.getInfo()` — the function returns one row per (ward, day-of-year)
    across all 366 days, so materialising the whole collection is wasteful. Use
    `thresholds.filter(ee.Filter.And(ee.Filter.eq("doy", 6), ee.Filter.eq("ward_id", "W-A")))` and route
    it through the existing `_props` helper.

    Threshold assertions use `pytest.approx(9.5, abs=1e-6)` and `pytest.approx(109.5, abs=1e-6)` — tight,
    because these are exact EE-computed values, not curve-fit approximations. The `[1..10]` p90 = 9.5
    expectation must carry an inline comment citing 03-RESEARCH.md Pitfall 3 and stating explicitly that
    numpy's 9.1 is the wrong oracle (D-02). Under no circumstances import numpy, scipy, statistics, or
    pandas into this test file, and do not write a Python percentile/pooling/day-of-year helper to
    compute an expected value — the whole point of D-02 is that the parallel implementation would
    silently diverge from the engine under test.

    Fixture date sets, stated once here so they are not re-derived: the wraparound collection is
    2019-12-27, 2019-12-28, 2019-12-29, 2019-12-30, 2019-12-31 (doy 361, 362, 363, 364, 365 — 2019 is
    not a leap year) plus 2020-01-01 through 2020-01-10 (doy 1-10); values are irrelevant to the two
    pooling tests, so use 1.0 for all fifteen rows and assert on row counts only.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k "climatology or pooling" -v 2>&amp;1 | grep -c "No module named 'heatwave.science.climatology'"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py --collect-only -q` collects exactly 14 tests with zero collection errors (5 from plan 03-01 plus 9 new)
    - All 9 new tests fail with `ModuleNotFoundError: No module named 'heatwave.science.climatology'` (RED confirmed)
    - The 5 CLIM-05 tests from plan 03-01 still pass: `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k zonal -x` reports 5 passed
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k pooling --collect-only -q` collects exactly 3 test ids
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k climatology --collect-only -q` collects exactly 6 test ids (test_climatology_module_exports plus the five threshold-behaviour tests)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "_make_ward_daily_fc"` returns at least 8 (definition plus use in all seven climatology/pooling data tests; the module-export and floor-mod tests need no fixture)
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "9.5"` returns at least 3
    - `grep -c "import numpy\|import pandas\|import scipy\|import statistics" tests/test_heatwave_detection.py` returns 0 (D-02)
    - `grep -F -c "9.1" tests/test_heatwave_detection.py` returns at least 1 and it appears only inside a comment or docstring naming it as the wrong numpy oracle — never inside an assert
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "inspect.signature"` returns 1
    - `grep -v '^#' tests/test_heatwave_detection.py | grep -F -c "2019-12-27"` returns at least 1 and `"2020-02-29"` returns at least 1
    - `heatwave/science/climatology.py` does not exist yet: `ls heatwave/science/climatology.py` exits non-zero
  </acceptance_criteria>

  <done>tests/test_heatwave_detection.py holds 14 tests; the 9 new ones encode D-05 (Feb 29 own slot), D-06 (bidirectional wraparound), the live-verified EE percentile oracle (D-02) and config-driven defaults, and all 9 fail solely because heatwave.science.climatology does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement heatwave/science/climatology.py wraparound pooling and grouped percentile thresholds (GREEN)</name>

  <files>heatwave/science/climatology.py</files>

  <read_first>
    - `heatwave/science/heat_index.py` — the exact module style to replicate: one-line docstring, `from __future__ import annotations`, `import ee`, `from heatwave.config import settings`, plain annotated top-level functions, no classes, requirement/decision ids cited in function docstrings (line 10's `D-01/WR-01` convention)
    - `heatwave/data/ingest.py` lines 27-28 — the `x = x or settings.…` None-defaulting convention this module's config-driven parameters must follow
    - `heatwave/config.py` lines 12-18 — the `ClimatologyConfig` field names
    - `.planning/phases/03-climatology-heatwave-detection/03-RESEARCH.md` — Pattern 3 (floor-mod + pooling filter, verified live), Pattern 4 (grouped percentile via `reduceColumns`, including the `groupField=1`-is-a-selector-index finding), Pitfall 2, Pitfall 3, and the Anti-Patterns list
    - `.planning/phases/03-climatology-heatwave-detection/03-PATTERNS.md` — the `heatwave/science/climatology.py` section: imports, config-usage pattern, both core patterns, and the anti-pattern to flag in comments
    - `heatwave/zonal.py` — the input row schema this module consumes
    - `tests/test_heatwave_detection.py` — written in Task 1; it is this task's acceptance oracle
  </read_first>

  <behavior>
    - `floor_mod(n, m)` returns a non-negative ee.Number for negative inputs: -3 mod 366 is 363, not -3.
    - `wrapped_day(n)` maps any integer onto 1..366: -3 -> 363, 0 -> 366, 1 -> 1, 366 -> 366, 367 -> 1, 730 -> 364.
    - `pooling_window_filter(doy, window_days)` returns a calendarRange day_of_year filter whose start and end are both in 1..366, relying on calendarRange's native wrap when start > end.
    - `compute_climatology_thresholds` returns one feature per (ward_id, doy) that has pooled baseline data, carrying `ward_id`, `doy`, `threshold`.
    - Rows outside [baseline_start_year, baseline_end_year] never contribute to any threshold.
    - With all four tuning parameters omitted, behaviour matches settings.climatology's configured values.
  </behavior>

  <action>
    Create `heatwave/science/climatology.py`.

    Module docstring: state that this module computes the per-ward, per-calendar-day percentile
    climatology baseline (CLIM-01) with a ±N-day pooling window (CLIM-02) over the per-ward-daily
    FeatureCollection produced by `heatwave/zonal.py`, and that day-of-year follows the 1-366
    WMO/ETCCDI convention (D-05) with pooling windows that wrap across the year boundary in both
    directions (D-06). Then `from __future__ import annotations`, blank line, `import ee`, blank line,
    `from heatwave.config import settings`.

    Define `DAYS_IN_LEAP_YEAR = 366` as a module constant and use it everywhere the modulus is needed —
    366 (not 365) because D-05 gives Feb 29 its own day-of-year slot.

    Define `floor_mod(n, m) -> ee.Number` implementing Python's floored modulo:
    coerce `n` with `ee.Number(n)`, take `r = n.mod(m)`, and return
    `ee.Number(ee.Algorithms.If(r.lt(0), r.add(m), r))`. The docstring must record why this exists:
    `ee.Number.mod()` uses a truncated, sign-follows-dividend convention, so `ee.Number(-3).mod(366)`
    returns -3 (verified live, 03-RESEARCH.md Pitfall 2), and feeding a negative value to
    `ee.Filter.calendarRange` raises `Filter.calendarRange: Start and end date values must be >= 0`.
    Never "fix" that error by clamping to 0 — the mod arithmetic is the fix.

    Define `wrapped_day(n) -> ee.Number` as `floor_mod(ee.Number(n).subtract(1), DAYS_IN_LEAP_YEAR).add(1)`,
    with a docstring giving the live-verified mappings -3 -> 363, 367 -> 1, 730 -> 364.

    Define `pooling_window_filter(doy, window_days) -> ee.Filter`: compute
    `start = wrapped_day(ee.Number(doy).subtract(window_days))` and
    `end = wrapped_day(ee.Number(doy).add(window_days))`, then return
    `ee.Filter.calendarRange(start, end, "day_of_year")`. Add a comment recording the research finding
    that `calendarRange` wraps natively when `start > end` (verified: `calendarRange(363, 5, 'day_of_year')`
    matched both Dec 30-31 and Jan 1-5), so combining two filters with `ee.Filter.Or` is unnecessary
    and must not be added — the only explicit wraparound handling needed is the floor-mod arithmetic
    above (D-06).

    Define `compute_climatology_thresholds(ward_daily_fc, percentile=None, window_days=None, baseline_start_year=None, baseline_end_year=None, value_property="value", ward_id_property="ward_id") -> ee.FeatureCollection`.
    Resolve each of the four None-defaulting parameters at the top of the body using the
    `heatwave/data/ingest.py` convention — `percentile = percentile or settings.climatology.percentile`,
    `window_days = window_days if window_days is not None else settings.climatology.pooling_window_days`
    (use the `is not None` form for `window_days` so an explicit 0, which the Feb-29 test passes, is not
    swallowed by falsiness), and the same `or` form for both baseline years. The literals 90, 5, 1991 and
    2020 must not appear anywhere in this file: they are typed, validated config
    (`heatwave.config.ClimatologyConfig`, REWORK-07) and hardcoding them is the config-drift tampering
    threat in 03-RESEARCH.md's Security Domain.

    Body, following 03-RESEARCH.md Pattern 4 (verified live). First filter to the baseline:
    `baseline = ward_daily_fc.filter(ee.Filter.calendarRange(baseline_start_year, baseline_end_year, "year"))`.
    This reads the year from `system:time_start`, which is why `heatwave/zonal.py` sets it.

    Then an inner `threshold_for_day(doy)`: coerce with `ee.Number(doy)`; pool with
    `baseline.filter(pooling_window_filter(doy, window_days))`; run
    `pooled.reduceColumns(reducer=ee.Reducer.percentile([percentile]).group(groupField=1, groupName=ward_id_property), selectors=[value_property, ward_id_property])`;
    read `ee.List(grouped.get("groups"))`; and map each group dictionary to
    `ee.Feature(None, {"ward_id": <group's ward id>, "doy": doy, "threshold": <group's p{percentile} entry>})`.
    Add a comment recording the verified finding that `groupField=1` is an index into the `selectors`
    list (index 1 = the ward id column), not a property name. The percentile value lives in the group
    dictionary under the key `"p" + str(percentile)` — build that key from the resolved `percentile`
    variable so a config change to, say, 95 keeps working; the emitted property is always named
    `threshold`, so downstream code never has to know the configured percentile.

    Return `ee.FeatureCollection(ee.List(ee.List.sequence(1, DAYS_IN_LEAP_YEAR).map(threshold_for_day)).flatten())`.

    Use `ee.List.sequence(1, 366).map(...)` with the grouped reducer — one reducer call per calendar day
    covering all wards at once. Do NOT write a nested `wards.map(ward -> days.map(...))` loop: that is
    O(wards x days) separate reducer calls and is the explicit anti-pattern D-04's ward-count-agnostic
    requirement rules out. There must be no Python-level loop over wards anywhere in this module.

    Add a short comment near the percentile reducer recording, per D-02 and 03-RESEARCH.md Pitfall 3,
    that `ee.Reducer.percentile()` does not match numpy's default interpolation (EE returns 9.5 for the
    sample 1..10 at p90; numpy's default `linear` method returns 9.1), so no numpy/pandas reference
    implementation of this math may be introduced here or in tests. Do not import numpy, pandas, scipy,
    statistics, or datetime in this module.

    Climatology output rows deliberately carry no `system:time_start` — they are joined on
    (`ward_id`, `doy`) by `heatwave/science/heatwave.py` and are never calendarRange-filtered. State this
    in the return-value docstring so a future reader does not "restore" a timestamp they think is missing.
  </action>

  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -v` reports 14 passed, 0 failed (with local credentials present)
    - `.venv/Scripts/python -m pytest tests/ -x -q` reports the full suite green
    - `.venv/Scripts/python -c "from heatwave.science.climatology import floor_mod, wrapped_day, pooling_window_filter, compute_climatology_thresholds; print('ok')"` prints `ok`
    - `grep -v '^#' heatwave/science/climatology.py | grep -c "settings\.climatology\."` returns 4 (percentile, pooling_window_days, baseline_start_year, baseline_end_year all resolved from config)
    - `grep -v '^#' heatwave/science/climatology.py | grep -c "\b90\b\|\b1991\b\|\b2020\b"` returns 0 (security V5 — no hardcoded climatology constants outside comments)
    - `grep -v '^#' heatwave/science/climatology.py | grep -F -c "DAYS_IN_LEAP_YEAR"` returns at least 3 (definition plus both wraparound uses)
    - `grep -v '^#' heatwave/science/climatology.py | grep -F -c "ee.Algorithms.If"` returns at least 1 (floor-mod correction present)
    - `grep -v '^#' heatwave/science/climatology.py | grep -F -c "groupField=1"` returns 1
    - `grep -v '^#' heatwave/science/climatology.py | grep -F -c "ee.Filter.Or"` returns 0 (native calendarRange wrap used, no redundant filter union)
    - `grep -v '^#' heatwave/science/climatology.py | grep -F -c "ee.List.sequence(1, DAYS_IN_LEAP_YEAR)"` returns 1
    - `grep -v '^#' heatwave/science/climatology.py | grep -c "import numpy\|import pandas\|import scipy\|import statistics\|import datetime"` returns 0
    - `grep -v '^#' heatwave/science/climatology.py | grep -c "^class \|for ward in \|\.getInfo()"` returns 0 (plain-function style; no Python-level ward loop; no client round-trip inside the computation)
  </acceptance_criteria>

  <done>heatwave/science/climatology.py exports the floor-mod helpers, the wraparound pooling filter, and the grouped per-ward percentile threshold computation, all fourteen tests are green including both wraparound cases and the Feb-29 slot, every tuning parameter resolves from settings.climatology, and the full suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-ward-daily table -> climatology thresholds | Multi-year ward-daily values (potentially containing nulls from sub-pixel wards, per plan 03-01's T-03-01) cross into the percentile aggregation that defines every heatwave threshold in the CHAP deliverable |
| `heatwave.config.settings.climatology` -> this module's parameters | The only source of percentile / pooling-window / baseline-year values; typed and validated at load (REWORK-07). Function parameters are supplied by in-repo callers only — there is no externally submitted input in this phase |
| Test runner -> GCP service account | Live Earth Engine calls against `heatwave-508110`, credential-gated exactly as in Phases 1-2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (config drift) | `compute_climatology_thresholds` tuning parameters | mitigate | ASVS V5: `percentile`, `window_days`, `baseline_start_year`, `baseline_end_year` default to None and resolve from `settings.climatology`; acceptance criteria assert exactly 4 `settings.climatology.` reads and zero `90`/`1991`/`2020` literals; `test_climatology_defaults_read_from_settings` proves the resolved defaults produce the configured p90 behaviour |
| T-03-08 | Tampering (silent wrong results) | `floor_mod` / `wrapped_day` wraparound arithmetic | mitigate | Negative day offsets are floor-modded before reaching `ee.Filter.calendarRange`, which otherwise raises `Start and end date values must be >= 0` or, worse, silently truncates the pooling window at the year boundary; `test_pooling_window_floor_mod_handles_negatives` plus both wraparound row-count tests are the regression guard (D-06) |
| T-03-09 | Tampering (wrong test oracle) | Percentile expectations in tests | mitigate | D-02: expected values are EE's own live-computed results (9.5 for the sample 1..10), never numpy's 9.1; acceptance criteria forbid numpy/scipy/statistics/pandas imports in both the module and the test file, so no parallel implementation can silently diverge from the engine under test |
| T-03-10 | Tampering (baseline contamination) | Baseline year filtering | mitigate | `ee.Filter.calendarRange(start_year, end_year, 'year')` applied before pooling; `test_climatology_baseline_year_filter_excludes_outside_years` proves 999.0 outliers dated 1999 and 2021 cannot move a 2000-2000 baseline threshold |
| T-03-11 | Denial of Service | 366 grouped reducer calls per invocation | accept | One reducer call per calendar day covering all wards (not O(wards x days)); verified live at test scale; Phase 4 scale is explicitly out of scope here per D-03 and is flagged in 03-RESEARCH.md Open Question 1 |
| T-03-12 | Information Disclosure / Spoofing / Repudiation / Elevation of Privilege | — | accept | Not applicable: this module performs pure server-side numeric aggregation, introduces no auth/session/logging/privilege surface, and reads no credential material (03-RESEARCH.md Security Domain marks V2/V3/V4/V6 as "No") |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No packages installed by this plan (03-RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies this phase). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k climatology -x` — 6 CLIM-01 tests pass with credentials (module export + five threshold behaviours), skip cleanly without
- `.venv/Scripts/python -m pytest tests/test_heatwave_detection.py -k pooling -x` — 3 CLIM-02 tests pass, covering both wraparound directions and the floor-mod fix
- `.venv/Scripts/python -m pytest tests/ -x` — full suite green, including plan 03-01's CLIM-05 tests and all Phase 1-2 regression tests
- `.venv/Scripts/python -c "import heatwave.science.climatology"` — module imports cleanly outside pytest
</verification>

<success_criteria>
- CLIM-01: a per-ward, per-calendar-day percentile baseline is produced from a multi-year ward-daily record, grouped by ward in a single reducer call per calendar day, restricted to the configured baseline years
- CLIM-02: the ±`pooling_window_days` window is applied around each calendar day and verified live in both wraparound directions (day 3 reaching back into December, day 366 reaching forward into January)
- D-05 is honoured and directly tested: day-of-year 60 is Feb 29 in a leap year, day-of-year 59 is Feb 28, and Feb 29 receives its own pooled threshold
- D-06 is honoured: wraparound is handled by explicit floor-mod arithmetic feeding calendarRange's native start>end wrap — never by truncation at the range edge, never by an `ee.Filter.Or` union
- D-02 is honoured: the percentile oracle is EE's own live-verified output, and no numpy/pandas/scipy reference implementation exists in module or tests
- D-04 is honoured: no Python-level ward loop and no hardcoded ward count — Phase 4 calls this function unchanged at 4,841-ward scale
- Security V5: all four climatology tuning parameters resolve from `heatwave.config.settings.climatology`
</success_criteria>

<output>
Create `.planning/phases/03-climatology-heatwave-detection/03-02-SUMMARY.md` when done
</output>
