services:
  heatwave-model:
    build: .
    ports: ["9090:8000"]
    volumes: ["heatwave_model_data:/work/data"]
    environment: {LOG_FORMAT: json, LOG_LEVEL: INFO, WORKERS: 1}
    restart: unless-stopped
volumes:
  heatwave_model_data:
